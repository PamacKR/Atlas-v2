{
  "name": "atlas",
  "version": "0.1.0",
  "description": "Atlas — a centralized academic knowledge platform",
  "private": true,
  "main": "dist/main/main.js",
  "scripts": {
    "build": "tsc -b && node scripts/copy-assets.js",
    "start": "npm run build && electron .",
    "watch": "tsc -b -w",
    "verify": "npm run build && node scripts/verify-app.js"
  },
  "devDependencies": {
    "@types/adm-zip": "^0.5.8",
    "@types/better-sqlite3": "^7.6.11",
    "@types/node": "^22.10.2",
    "electron": "^33.2.1",
    "electron-builder": "^25.1.8",
    "playwright": "^1.61.1",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "adm-zip": "^0.6.0",
    "better-sqlite3": "^11.7.0",
    "chokidar": "^4.0.3",
    "mammoth": "^1.12.0",
    "marked": "^12.0.2",
    "xlsx": "^0.18.5"
  }
}
