node_modules/
dist/
*.log
.DS_Store
Thumbs.db

# Local build artifacts / packaged app output
release/
out/
tsconfig.tsbuildinfo

# Runtime data lives in Downloads/Atlas-Storage (a separate folder from this
# repo, see ARCHITECTURE.md §2) and should never appear here, but these are
# kept as a defensive backstop in case anything is ever run with a stale path.
atlas.db
atlas.db-shm
atlas.db-wal
/files/

# Output screenshots from Playwright/Electron verification scripts
verify-*.png

# Local Git snapshots are not source or release artifacts.
*.bundle

# UI mockup screenshots — regenerated on demand, not source of truth
mockups/*.png
