# Atlas — Status

Living snapshot of where the project actually is. This is the first thing to read (after `AGENTS.md`) in a new chat or after context compaction — it should be possible to resume correctly from this file alone plus the other docs it points to, without the user having to re-explain anything.

**Last updated:** 2026-08-03 (UI overhaul in progress on `ui-overhaul-v2`; the current-session section below supersedes historical notes.)

## Next session — start here

## Current session — 2026-08-01

UI overhaul work is active on `ui-overhaul-v2`; the earlier statement below that no implementation exists is historical and superseded by this section.

- Shared Direction A tokens and the rebuilt sidebar shell are complete. The sidebar’s icon column stays fixed between expanded and collapsed states.
- The Dashboard has been structurally rebuilt toward `mockups/dashboard-a.html`: the date/search share the top row, courses render as a five-column horizontal strip with colour swatches and aggregate metadata, and compact unboxed lists replace dashboard filter chips and card separators.
- The global default accent is now the mockup amber (`#d9a441`), so a legacy persisted blue setting no longer applies.
- `npm run build` and the full Electron `npm run verify` passed after this dashboard change. The Electron CSP warning is pre-existing development-mode output.
- Dashboard/sidebar cosmetic mismatches reported on 2026-08-01 (collapsed-rail hover geometry, scrollbar corner rendering, and other minor spacing polish) are deliberately deferred at the user's request. Do not continue patching them until the final whole-app fidelity pass; the user's running app did not reflect the attempted CSS changes reliably.
- Dashboard deadline overlays are now being rebuilt directly from `mockups/overlays-a.html`: past deadlines are excluded from Dashboard Up Next; the deadline viewer has the mockup’s structured header, facts, footer, reset action, and interaction states; and the editor uses one native date-time field instead of separate date/time boxes. `npm run build` passed after the latest cohesive overlay control pass.
- Classroom conflict UI is context-safe: only a live Classroom-synced deadline that has local overrides can show a Classroom-version/reset action. A Classroom removal notice is mutually exclusive with that action, and manual deadlines can never be changed by the reset IPC handler.
- Fixed the live-renderer cause of stale Classroom notices: `.deadline-conflict-notice` forced `display: flex` even when JS set `hidden`, so manually created deadlines showed notices despite correct data and eligibility logic. It now uses `:not([hidden])`; the reset button has an explicit hidden safeguard too.
- Deadline Kind and Due controls now use Atlas-owned themed controls instead of native OS-rendered select/date-time inputs. The Due popover accepts `DD-MM-YYYY` and optional `HH:MM`, validates actual calendar dates and 24-hour time before saving, and supports clearing the date. `npm run build` passed; the full verification suite was intentionally not run for this focused control replacement.
- Dashboard’s Manage, Calendar, and Notes navigation actions use the mockup’s text-link treatment only: no button surface, border, or hover background; hover changes text colour only.
- Note editor overlay was rebuilt against `mockups/overlays-a.html`: 880×640 panel, shared overlay header with course/save metadata and icon actions, editable title and origin metadata in the body, plus an unboxed scrollable editor area. Existing autosave, scan/OCR, assignment, fullscreen, and close behavior retain their existing IDs and wiring. `npm run build` passed; full verification is deferred until this overlay pass is complete.
- The user reaffirmed that `mockups/overlays-a.html` and `mockups/controls-a.html` are mandatory shared contracts for every remaining UI pass. `AGENTS.md` now records the standard mockup-to-app workflow, including exact panel/control anatomy, text-link vs button distinction, functional states, and whole-surface review.
- Note overlay metadata was consolidated into one header: editable note title, then course, authoring origin, and save state in one subtitle. The repeated body title/meta rows were removed. `npm run build` passed.
- Real `Downloads/Atlas-Storage/atlas.db` was inspected after the user reported stale Classroom controls. Its manual deadlines have no Classroom ID, no override, and `classroom_removed = 0`. No real deadline data was modified during diagnosis. The user runs the source app with `Launch Atlas.bat`; do not redirect UI diagnosis toward installers or packaged builds unless explicitly asked.
- Courses list surface is now rebuilt against `mockups/courses-a.html`: literal title/count and term grouping, text-only term and sort controls, minimal 30px view toggles, unboxed rows, and real per-course upcoming-deadline data (past/completed work excluded). The add/import actions remain functional text actions; the new-course form uses Atlas-owned text fields rather than a native term select. `npm run build` passed. Next: rebuild the course-detail workspace directly from `mockups/course-detail-a.html`, then review the complete Courses surface before moving to Resources.
- Course-detail Overview is now rebuilt against `mockups/course-detail-a.html`: text back link, course identity header, underlined tabs, unboxed two-column Resources/Notes/Up-next layout, and a Classroom status row. Up next uses live, non-past, incomplete deadlines and opens the existing deadline viewer; existing edit, archive, export, Classroom, file, and preview behaviours remain wired. `npm run build` passed. The remaining course tabs still need their final literal row/tile pass before Courses is declared complete.
- The remaining course-detail tabs now share the same unboxed row/tile treatment: Deadline rows, Classroom feeds, and watched-folder rows no longer retain the pre-overhaul card treatment. Course list, detail Overview, and detail tabs have each passed `npm run build`; a full Electron verify remains intentionally deferred until the complete Courses milestone is reviewed.
- Course review feedback pass: removed the unnecessary Term/Name/Files controls; grid/list icon order is grid first and list second on the Course list, Resources, and Deadline controls; widened the import/add-course action spacing; grouped detail actions at the right; removed horizontal overflow from Course detail; added archive confirmation via the shared in-app overlay; themed deadline checkboxes; constrained announcement previews to one line; and rebuilt the course-edit overlay around the shared overlay/control tokens. `npm run build` passed. The exact provider-side Classroom display name is not currently persisted in the course record (only its ID); this remains to be added rather than falsely displaying the local course name as the provider name.
- Correction after user feedback: the generic confirmation dialog itself was still legacy-shaped, so the archive/disconnect confirmations could not meet the shared overlay contract. It is now rebuilt with the `overlays-a.html` warning icon, panel geometry, body and footer actions; the confirm button dynamically says Archive, Disconnect, Delete, Reset, or Confirm rather than always saying Delete. `npm run build` passed.
- Real-data repair, 2026-08-01: removed the mistaken Behavioral Finance watched-folder record (`Downloads\\investing`, ID 4) and the eight resources it imported (IDs 16042–16049) from `Downloads/Atlas-Storage`. Verified afterward: zero matching watch records and zero matching imported resources. The external `investing` folder itself was not changed. The Watch folder control is a neutral bordered button (not a yellow primary action and not a text link), per the course-detail mockup treatment.
- Course-detail correction: the Overview horizontal scrollbar came from the generic Dashboard widget's `overflow-y: auto` rule (which implicitly enables horizontal auto overflow), not page overflow. Course Overview lists now explicitly use visible overflow with no height cap. Deadline view toggles are neutral rather than accent-filled, Add deadline is a neutral bordered button, and Classroom attachments use compact monochrome document chips. `npm run build` passed.
- Shared icon pass started: resource grid tiles, deadline list/grid glyphs, and Classroom attachment controls/detail attachments now use minimal monochrome inline SVGs rather than emoji. Course swatches and urgency colours are intentionally retained because they carry course/status meaning. `npm run build` passed. Continue this audit while rebuilding remaining pages rather than reintroducing emoji utility icons.
- Deadline view correction: deadline grid uses the same continuous border-grid language as Courses (not rounded floating cards), checkbox pinned top-right, and icon/title anchored at top-left. Deadline list reserves a 178px non-wrapping due-date column and reduces kind width so dates do not split onto two lines.
- Dashboard heading correction: weekday and date are rendered as separate spans; the date is muted and normal-weight while the weekday remains the heading weight, matching `dashboard-a.html`. `npm run build` passed.
- Resources main surface is rebuilt directly from `mockups/resources-a.html`: a live-count course filter rail, kind and sort text controls, grid-first/list-second neutral view toggles, real This week/Earlier grouping, aligned continuous file rows, and hairline grid tiles. The existing upload, drag/drop, course filtering, sort, preview, and context-menu behaviours remain wired. `npm run build` passes. `scripts/verify-app.js` has been updated away from removed native controls and old list markup, but its full end-to-end run still needs to be completed and screenshot-inspected before this page is declared fully verified.
- Resource preview now uses the shared `overlays-a.html` anatomy as well: the large 1040×720-style panel, structured title/subtitle header, 30px monochrome utility actions, red destructive-close hover, canvas body, and rounded floating zoom controls. Existing preview, OCR, Drive, and fullscreen functionality is retained. `npm run build` passed.
- Resources correction pass: the Upload file picker now follows the shared small overlay shell, with the drop area above a swatch-backed course assignment list in upload mode rather than the old search-first layout. Preview actions are explicitly held in the header's right side, and image zoom controls now live as a compact floating group inside the preview canvas rather than taking a full-width outlined row. `npm run build` passed.
- Upload flow correction: the file is now chosen or dropped first, then the picker enables its course assignment list; selecting a course uploads that exact pending file. The upload close glyph was reduced to the shared icon-action scale. `npm run build` passed.
- Launch correction: `Launch Atlas.bat` now runs `npm run start:fast` rather than rebuilding before every launch. The normal development workflow already builds after each source change, so everyday launches no longer pay the full TypeScript/renderer build cost. README instructions reflect this.
- Notes main surface is now being rebuilt against `mockups/notes-a.html`: title/count and real actions, a note-count course rail, agent-only rail filter, literal text sort controls, grid-first/list-second controls, real recency groups, continuous note rows, and excerpt tiles. Existing note creation, scan import, filtering, context menu, and editor-opening behavior remains wired. `npm run build` passed. The Notes overlay/editor still needs its whole-surface review before Notes is complete.
- Notes refinement: its title and actions now occupy the shared top/search row (title left; actions immediately left of search) rather than consuming a separate page row. Both actions use the mockup's neutral bordered button treatment, and the Notes course rail is widened to keep long course names readable. This establishes the top-row pattern to carry through the remaining page passes. `npm run build` passed.
- Correction to the top-row pass: Courses, Resources, and Notes now all use the same structural topbar contract — page title at left; page-specific actions immediately to the left of the shared search field. It is no longer Notes-specific. The old high-specificity New note accent rule is explicitly overridden to keep both Notes actions neutral, and the Notes rail now permits long course names to wrap instead of clipping. `npm run build` passed.
- Topbar regression fixed: `.topbar-page-actions` had an unconditional `display:flex`, which overrode `hidden` and showed every page's actions at once. It is now `:not([hidden])`, per the project's hidden-selector safeguard. All topbar actions have an explicit neutral bordered style. Notes rail course labels are now wrapped label elements with `overflow-wrap`, not anonymous flex text that could overflow. `npm run build` passed.
- Topbar colour correction: legacy ID styles for Add course and Upload file were still more specific than the shared control rule, leaving amber buttons in the new topbar. The shared rule now deliberately outranks those IDs, so Courses, Resources, and Notes use one neutral bordered action treatment. `npm run build` passed.
- Notes/topbar correction: reverted the Notes rail to its compact 200px filter width; long names are safely ellipsized within it rather than wrapping and expanding the surface. The New note button has an explicit neutral hover/focus transition. Non-dashboard topbars now top-align their title/actions/search so the search field begins at the same vertical position as the Dashboard search. `npm run build` passed.
- Notes list correction: the long-name problem was in the right-aligned course/time field of list rows, not the filter rail. Its fixed 170px width has been replaced with a 330–430px responsive column so course names remain visible alongside the relative timestamp. `npm run build` passed.
- Notes list refinement: restored the compact course/time column rather than widening the layout. Course name and timestamp are now separate elements, so the course name truncates with an ellipsis like the sidebar while the timestamp remains readable. `npm run build` passed.
- Calendar has been rebuilt directly from `mockups/calendar-a.html`: the title now uses the shared top/search row; a continuous fixed six-week month table replaces the old card grid; real deadline and announcement data render as course-swatch event lines; and the mini calendar, custom type/course filters, upcoming list, and Month/Week/Day views are all functional. A dedicated `scripts/verify-calendar.js` now validates the 42-cell month/mini grids, view switches, and persistent filters in a throwaway Electron data directory and saves `verify-calendar.png` for visual review. `npm run build` and that focused Calendar check pass. The broad `npm run verify` suite still stops in an unrelated older flow after the course-detail stage, before it reaches Calendar; its initial missing Spring fixture was repaired in this session, but the remaining suite issue needs separate investigation rather than being mislabeled as a Calendar regression.
- Calendar regression correction: `#page-calendar` and the Month view had unconditional `display:flex` rules, so they overrode the `hidden` attribute and leaked into every page/Calendar view. Both now use `:not([hidden])`; the Calendar main area is a non-scrolling flex surface, and the old ID-level grid background/gap is explicitly neutralized so cells remain unfilled with only hairline borders. The focused Calendar test now asserts Calendar hides after page navigation and Month hides in both Week and Day views. `npm run build` and focused Electron Calendar verification pass.
- Settings is now rebuilt against `mockups/settings-a.html`, `controls-a.html`, and `overlays-a.html`: a compact section rail (Appearance, Sources, Shortcuts, AI & Integration, Storage, About) drives one hidden-safe panel at a time, and all Settings controls use Atlas-owned bordered controls rather than native selects. Sources retains its existing Drive/Classroom/sync functionality but uses the mockup's row anatomy; the responsive storage panel reports actual managed-file/database sizes, per-course storage, extraction status, and retained backups.
- Settings now has real agent and backup behavior, not placeholder UI. The shared `agentAccess` setting is checked before every MCP read/write tool call and before either MCP mutation, so turning the switch off immediately blocks a separate connected agent. Local `atlas.db` backups are retained in `Atlas-Storage/backups/`; the user can choose daily, weekly, or off scheduling (which runs on Atlas startup and then checks hourly while Atlas is running) and can make an immediate backup. Restore is deliberately not presented as an action yet because safely replacing the live database requires a controlled restart flow, not an in-place overwrite.
- `scripts/verify-settings.js` validates all six panels' hidden state, the agent switch, the Atlas-themed Drive and backup dropdowns, and storage rendering in a throwaway data directory. `npm run build` and the focused Settings Electron check pass; the Settings and Sources screenshots were inspected.
- Settings refinement: accent swatches now update both the legacy and Direction A accent tokens, so the selected colour reaches every remaining UI surface rather than only older selectors. Sources' sync controls and action rows were re-aligned to the mockup's shared control anatomy; Settings navigation is sticky while a long panel scrolls; Shortcuts uses the mockup's compact uppercase heading/section-label language, neutral reset control, and application font keycaps; and the MCP configuration retains literal multiline indentation. Storage now gives the total/legend the mockup's separation and exposes a context-aware Review action only when files have an `empty` extraction result; it opens the existing Resources surface filtered to those files. About uses the mockup's uppercase heading and muted body colour. `npm run build` and the focused Settings Electron check pass after this correction.
- Settings follow-up: Source buttons now use the shared flex-centred control treatment, eliminating baseline drift; the duplicate Sources text-extraction block was removed (Storage remains the single extraction-status/review surface); and the Direction A palette now includes its literal blue swatch. The common `settings-button` rule has an explicit `[hidden]` safeguard, so a conditionally absent control such as Extraction Review cannot leak into the page. `npm run build` and focused Settings Electron verification pass.
- Settings Sources/review pass: accent-derived soft and border tokens now follow the selected swatch, so active/tinted controls no longer retain the default amber hue after choosing blue, purple, green, or pink. All Source actions now deliberately override their older per-ID styles to use the same neutral 34px Direction A buttons; Office previews has one boundary line instead of a redundant row divider. The Drive new-files review was rebuilt as the shared `overlays-a.html` panel: header/subtitle/close anatomy, custom Atlas selects, themed checks, and readable file rows while preserving individual and bulk Import/Ignore behavior. `npm run build` and the focused Settings Electron check pass; Sources screenshot inspected.
- Overlay correction: the Drive review list no longer uses generated file IDs to locate its internal controls, so a pending Drive record cannot prevent the entire row list from rendering after the count is shown. Classroom announcement/item detail and the Ctrl+/ shortcut sheet now use the same shared overlay structure, typography, close action, scroll body, and neutral controls. Announcement attachments are compact monochrome chips with hover/focus feedback rather than raw browser/default-looking content. `npm run build` and focused Electron Settings/shortcut verification pass; shortcut screenshot inspected.
- Classroom/backup follow-up: the new-courses review now uses the same shared review-overlay anatomy and Atlas-owned select controls as Drive, retaining individual and bulk Confirm/Ignore behavior. Storage backup rows expose confirmed Delete actions for individual copies plus a confirmed Delete all action. Both IPC handlers validate backup filenames and delete only inside the local backups directory. The focused Settings check creates and then deletes a throwaway backup end-to-end; `npm run build` passes.
- Backup spacing refinement: the Delete all heading now includes the same inter-row gap before the first individual Delete button as exists between individual backup rows. No behavior changed.
- Navigation/roadmap correction: Calendar rework is marked complete in `ROADMAP.md`: the delivered Month/Week/Day views, mini navigation, type/course filters, and focused Electron coverage satisfy the fast-follow scope. The sidebar Settings glyph is now a monochrome outlined cog, using the same stroke treatment as the other rail icons.
- Search-results overhaul: the global dropdown now follows `mockups/search-a.html`'s continuous panel anatomy. It presents the backend's fixed Courses/Names/Content/Classroom ordering as visible sections, provides course-filter chips, uses source badges, and groups document-part hits beneath their parent resource with expandable page snippets. Search results carry the resource source and parent title from the main process so Drive files and page-hit groups are represented accurately; mouse and keyboard selection continue to open the original target. Existing broad Electron verification reaches an unrelated historic stop before its search section, so the search assertions were updated to target real result rows and this surface still needs its own focused Electron check plus screenshot inspection before it is declared complete.
- Search/polish follow-up: choosing a search course chip now stops its click from reaching the document-level click-away handler after the chip is synchronously re-rendered, so the dropdown remains open. Legacy `li:hover` styling is explicitly neutralized on search sections; only result rows retain the rounded hover state. Resources list glyphs are flex-centred to eliminate their baseline lift. **Deferred as incomplete:** despite repeated CSS attempts, the actual user-launched app still shows square scrollbars and Dashboard row hovers without the requested padding/rounded geometry. This is recorded in `ROADMAP.md` as a dedicated visual-fidelity debt item; do not claim it fixed until it is diagnosed against the live rendered cascade.
- Dashboard v2 is implemented and awaiting user visual review: a new `dashboard_cleared_items` table records Classroom announcements/assignments cleared from the dashboard. Its one-time baseline silently clears all Classroom content that already existed when this upgrade first ran, so only subsequently synced items appear as new. The existing Announcements dashboard section now shows those new Classroom updates, including an item-kind label, a per-row check action, and a filter-aware text-only Mark all read action. Opening a row still opens its existing Classroom detail/assignment overlay. It also supports user-created **important announcements**: the user chooses a course and writes the announcement in the shared themed overlay; it stays pinned on Dashboard even after all Classroom updates are marked read, and its remove action only unpins it from Dashboard rather than deleting the course record. `scripts/verify-dashboard-v2.js` launches the real Electron app with throwaway data and passes the baseline/new-item, overlay creation, individual-clear, mark-all, persistent-pin, and unpin paths. Keep `ROADMAP.md` unchecked until the user has reviewed this visual surface.

**Historical state follows.**

**What actually happened (read this before assuming any earlier plan is still live):**

1. **A full 14-stage implementation plan was written, approved, and then actually built** on branch `ui-overhaul` — real code changes landed in `src/renderer/*` for Stages A through L (token layer, shell/primitives, Dashboard, Courses+detail, Resources, Notes, Calendar, Settings, search overhaul, new Settings features, empty/first-run states).
2. **The user then did a rigorous, literal side-by-side comparison** — screenshots of the real running app against the mockup HTML files, page by page — and found the implementation did not match the mockups in many real, confirmed ways: an invented accent-color palette instead of the mockup's actual hex values, a shared `.chip` class silently reused for two incompatible mockup patterns (a bordered pill in Settings vs. a plain-text tab in Resources/Dashboard), a `.link-button` component that rendered as a boxed button instead of the mockup's plain text, dozens of hardcoded `border-radius: 4px` values instead of the token scale, several buttons with no styling at all, and features present in the mockup HTML that were simply never transcribed into the app (Calendar's Filters panel, the search dropdown's course-filter chips, Resources' recency grouping, Courses' term-grouped sections). Root cause, in the user's own words: *"you simply got the code wrong"* — values and markup were being re-derived from memory/summary instead of copied directly out of the mockup files.
3. **Decision: abandon that implementation rather than keep patching it.** `ui-overhaul` still exists on disk with all of that work (26 commits) — **do not merge it or build on top of it**, it's kept only as a reference for what was tried. A fresh branch, **`ui-overhaul-v2`**, was created from `3c5f9cc` ("Record approved UI-overhaul implementation plan; no code touched") — the exact commit right after the six original mockups were finalized/approved and *before* Stage B touched any real app file. `ui-overhaul-v2` is the live branch now.
4. **`ui-overhaul-v2` was then brought current on mockups only** — the full, later mockup set from `ui-overhaul` was copied over (`course-detail-a.html`, `overlays-a.html`, `search-a.html`, `controls-a.html`, `dashboard-light-a.html`, `settings-light-a.html`, plus refined versions of the original six: `courses-a`, `dashboard-a`, `notes-a`, `resources-a`, `settings-a`). Nothing else from `ui-overhaul` was brought over — `src/renderer/*` on `ui-overhaul-v2` is untouched, identical to before any overhaul work began.
5. **The old 14-stage plan document is gone from the workflow** — it lived outside this repo, at a Claude-Code-specific path (`~/.claude/plans/vectorized-gliding-sketch.md`), and does not carry over across a tool switch. Treat it as no longer authoritative. If a written plan is wanted again, write it fresh against the current mockup set and store it somewhere that survives a tool/session change (this repo, if it needs to be durable).
6. **The hard-won lesson for whoever builds this next**: implement by opening the actual mockup HTML/CSS files and copying markup structure and literal values (hex colors, class names, spacing) directly — don't reconstruct from memory, a summary, or "what a design like this usually looks like." Every real defect found in the abandoned attempt traced back to that shortcut.
7. **A real app icon** is still separately pending — unrelated to the above, still just a one-line `build.win.icon` swap once a logo exists (`ROADMAP.md`).

**Known loose end, unrelated to the UI work, not touched:** a stray git worktree at `.claude/worktrees/goofy-noyce-e28e2e` (branch `claude/goofy-noyce-e28e2e`, stuck at old commit `a68d21e` "Build Phase 4 Part A: page-aware text extraction", with an uncommitted change to `src/renderer/styles.css` sitting in it). Predates this session's work; not understood to be related to anything current. Flagged here rather than silently removed or assumed safe to delete — needs the user's call.

**Work on `ui-overhaul-v2` should continue one commit at a time**, same as before, merged to `main` only once a real implementation is done and verified against the mockups directly.

## Session 2026-07-29 (continued) — the two ready-to-build fixes from last session

Both items flagged as "ready, design-independent" at the end of the previous session are now built and verified.

**1. Search dead-click fix (`phase5-spec.md` §2.2).** `openSearchResult()` (`renderer.ts`) only handled `note` and `resource` — `document_part`, `announcement`, and `assignment` results silently did nothing when clicked, which was the large majority of hits once Phase 4's page-aware extraction started indexing 3,067 individual pages. Fixed by wiring the three missing branches to handlers that already existed:
- `document_part` → opens its parent resource's preview. `search:query`'s SQL now `LEFT JOIN`s `document_parts` to include `resourceId` alongside each hit (only populated for `document_part` rows).
- `announcement` → fetches the course's Classroom content and opens the existing announcement detail overlay.
- `assignment` → same, via the existing `openAssignmentDetail` (which itself prefers the mirrored deadline viewer over the generic overlay).

No new UI, no new IPC endpoints — every handler this needed was already built for the Classroom content tabs; this was purely reusing them from the search results list. This is a *functional* renderer fix, not a cosmetic one, so it doesn't conflict with the "don't patch the UI" standing rule.

**2. Packaging + startup fix (`phase6-spec.md` §4, §5).** Two changes:
- Configured `electron-builder` for real (`package.json`'s new `build` key: `appId`, `productName: "Atlas"`, NSIS target, `asarUnpack` for native `.node` files). `npm run package:win` now produces a real signed-if-configured installer (`release/Atlas Setup 0.1.0.exe`) that installs a real `Atlas.exe` with its own Start Menu/desktop entries — no icon yet (electron-builder's own default is used), since the user is supplying a logo concept themselves and none exists. Verified end-to-end: built, packaged, launched the packaged `.exe` directly (confirms `better-sqlite3`'s native binding gets rebuilt correctly for the packaged Electron ABI, not just the dev one), then cleaned up the test launch. Added `npm run start:fast` (`electron .` with no rebuild) for quick dev relaunches when nothing's changed.
- `rebuildSearchIndex()` was running unconditionally on every launch (3,443 rows at the user's real scale). Added `searchIndexNeedsRebuild()` — a handful of `COUNT(*)` comparisons between `search_index` and its five source tables — so launch only pays for the full rebuild when something actually changed while the app was closed. Every other call site that mutates searchable content still calls `rebuildSearchIndex()` directly and is unaffected.

All four verify suites (`verify`, `verify:mcp`, `verify:extraction`, `verify:remote`) re-run and pass after both changes.

**A third, real bug found immediately after** — the user reported the packaged app opening as "Not Responding" (blank, unstyled Windows chrome) for a few seconds before rendering, when launched via the Start Menu shortcut against their real data. Root cause: `reconcileWatchedFolder()` and `reconcileCourseStorage()` (both run once per watched folder / per course at every launch, from the `app.whenReady()` startup loop) each called `rebuildSearchIndex()` **unconditionally** — so with the user's real course count, the *entire* multi-thousand-row search index was fully deleted and reinserted several times in a row, synchronously, before the window could process a single message. That's a genuine pre-existing bug (predates this session), just never visible until the packaging work ran startup against the real data directory for the first time instead of a small verify fixture. Fixed both functions to only call `rebuildSearchIndex()` when they actually deleted a resource, same principle as `searchIndexNeedsRebuild()` above. Re-ran all four verify suites; `atlas_list_resources reports part count` failed once and passed on 3 immediate reruns — confirmed the same pre-existing ordering flake already noted here, not caused by this change.

**Course search added, then corrected same session.** First pass only added course-name matches ranked above the existing (unpartitioned) FTS results — that left file-name and in-file-content matches still blended together in one FTS-ranked list, which is not what the user asked for and not what `phase5-spec.md` §2.3 actually specced. The user called this out directly. Rebuilt `search:query` properly as four hard-partitioned sections, each its own query: **Courses** (plain `LIKE` match, unchanged from the first pass) → **Names** (file/note titles only, via FTS5's `title:` column filter) → **Content** (page/slide/sheet hits, plus any resource/note that matched by body but not title, de-duplicated against Names) → **Classroom** (announcements/assignments). All four verify suites re-run and pass. Still not done, and out of scope for this pass: visual section headers in the results dropdown (a UI change, deferred per the standing no-UI-patching rule) and collapsing multiple page hits under one file (`phase5-spec.md` §2.4, still a "Large" item).

## Handoff — how the remote-attachments work landed

**`remote-attachments-spec.md` is built and the user has reconnected Google Classroom** (the step needed for the new `drive.readonly` scope, see below). What shipped, in build order:

1. **OAuth scope fix (a real discovery, not in the original spec):** `googleAuth.ts`'s `CLASSROOM_SCOPES` had zero Drive scope on it — a Classroom attachment's Drive file is shared by the professor with the account connected *to Classroom*, not the separate personal-account Drive connection, so the old assumption ("drive.readonly already covers this") was wrong. Added `drive.readonly` to `CLASSROOM_SCOPES`.
2. `extractMaterialLinks` (googleClassroom.ts) now keeps the Drive file ID and material kind (`driveFile`/`youTubeVideo`/`link`/`form`) instead of discarding them.
3. Schema: `resources` gained `remote_source`, `remote_ref`, `remote_mime_type`, `remote_fetched_version`, `link_kind`, `local_twin_id`, `parent_resource_id`, `discovery_depth`.
4. New `remoteFetch.ts` — a Drive fetcher: `canDownload` pre-check, Google-native export via `exportLinks` (falls back to `files.export()`), binary files streamed to a temp file (never under `Atlas-Storage/files/`), tries the Classroom OAuth connection first, falls back to the personal Drive connection.
5. New `remoteSync.ts` — orchestrates it all: local-copy-first resolution (§3.3), link-following with real caps (depth 2, 100 children/parent, 300/course/sync, cycle-safe dedupe by Drive file ID), failure-state handling, wired into Classroom sync + launch backfill with visible progress.
6. Exam-scale tool changes: `atlas_course_briefing` now reports totals alongside its top-20 lists; `atlas_read_document` called with no `from`/`to` returns an outline; course-scoped `atlas_search` ceiling raised from 25 to 100.
7. New `scripts/verify-remote.js` (`npm run verify:remote`) — passing, alongside `verify`, `verify:mcp`, and `verify:extraction`.

**Two more real bugs found and fixed right after, while checking in on the ECO-2202 sheet specifically** (the user reported it missing from its announcement in the app):
- **`classroom:getCourseContent` never joined announcement attachments at all** — only assignments and classwork did; the renderer hardcoded `links: []` for every announcement. The sheet was correctly synced and in the database the whole time; the UI just never had a code path to show it. Fixed both the IPC handler and the renderer.
- **Every Classroom link resource synced *before* this feature shipped** (the vast majority of the user's real 155) had `remote_source`/`link_kind` stuck at `NULL` and `extraction_status` forced to `'unsupported'` forever, since a later sync's `INSERT OR IGNORE` never revisits an already-present row. Added `backfillPreExistingRemoteAttachments()`, a one-time launch pass that converts them so the new fetch path actually picks them up. Also fixed a latent bug this surfaced: `driveFileIdFromUrl` would have mistaken a Google Forms `viewform` URL's `/forms/d/e/...` path for a real Drive file ID.

**Remote extraction has since completed against the user's real data — measured 2026-07-29, it worked:**

| | Before this work | After |
|---|---|---|
| Resources with readable text | 38 | **138** |
| Classroom Drive attachments readable | 0 | **108** |
| Indexed pages/slides/sheets | ~35 | **3,067** (3,032 of them from Classroom files) |
| Still pending | — | **0** |

One genuine failure out of 3,000+ (a link-following false positive, see below), 9 scanned PDFs with no text layer (`open-questions.md` #29), and 9 correctly-unsupported YouTube/Form links. The ECO 2202 index sheet the user asked about extracted successfully.

**Phase 5 rescoped 2026-07-29 by user decision, and specced:**
- **Dropped, not deferred:** relationship editing ("i do not see myself using this") and the offline-mode audit ("95% of the time i will be using this app online").
- **Kept and specced in the new `phase5-spec.md`:** a search overhaul (fixes a live bug where announcements/assignments/page-hits are all dead clicks, plus sectioned results so 3,067 page entries can't flood the list), Dashboard v2 (unread announcements + new assignments, both clearable — the user's explicit requirement), and configurable keyboard shortcuts with a `?` cheat sheet.
- **Explicitly rejected:** showing Classroom/Drive file *text* inside Atlas. The user prefers opening the real file in Drive — better fidelity, and it makes the file's origin unmistakable. Search still reads inside those files; it just hands off to Drive to view them.
- **Nothing built yet** — `phase5-spec.md` is drafted and awaiting a go-ahead.

**Phase 6 added and specced 2026-07-29 (`phase6-spec.md`)** — the functional product is essentially done, so this phase is about Atlas feeling like a finished desktop app: a full UI overhaul, settings redesign, design-style themes (brutalism/neomorphism/minimalism, not just colors), real app packaging, and startup performance. Two findings from that audit are worth carrying forward because they're structural, not cosmetic:

1. **The slow startup and the `.bat`-file launcher are the same problem.** `npm start` runs a *full TypeScript build* before opening the window, every single launch — and `electron-builder` is installed but has never been configured (no `build` key, no config file, no icon). Packaging the app properly fixes both at once.
2. **Design-style theming is impossible without a token layer, and that layer has to be built *during* the redesign.** All 106 `border-radius` and 119 `font-size` values in `styles.css` are hardcoded; the existing 68 CSS variables are essentially all color. A brutalist/neomorphic theme differs in radius, shadow, border, and type — none of which are variables today. Redesigning first and tokenizing later means two full passes over 3,500 lines of CSS.

Also flagged: `rebuildSearchIndex()` deletes and re-inserts all **3,443** indexed rows on every launch. `ARCHITECTURE.md` §14 justified that at "tens to low hundreds of rows" and explicitly said to revisit if the scale assumption broke — Phase 4's extraction broke it (~380 → 3,443 rows, still growing).

**Not touching the old Atlas v1 desktop shortcut** without explicit confirmation, even though the user suggested removing it — it's a real file outside this repo and v1 may still be in use.

**Gmail is now explicitly out of scope for Atlas** (2026-07-29 user decision) — see `ROADMAP.md`'s "Explicitly out of scope" section and `open-questions.md` #28. If ever built, it's a separate standalone tool, not part of this project. Also this session: added a description to Settings → About, refreshed README.md (was still describing Phase 1 scaffolding), and fixed the GitHub repo's "About" description (was still referencing an old "Pi + NVIDIA NIM" fork framing).

**Still out of scope, by design:** fetching arbitrary non-Drive web links, YouTube transcripts, writing back to Drive.

**A mistake made in an earlier session, corrected then**: an old smoke test wrote a stray `course-profiles/Microeconomics.md` into the user's *real* Atlas-Storage. Deleted, and this session's own `scripts/verify-remote.js` was double-checked for `ATLAS_DATA_DIR` isolation before ever running.

## Session 2026-07-29 (continued) — course editing (rename/re-code/re-term)

Atlas had no way to rename a course at all — a real gap, asked for directly by the user right after Phase 4 shipped. New "Edit" entry on the course card's right-click menu, and an "Edit course" button on the course detail page, both opening the same small modal (name/code/term, pre-filled) backed by a new `courses:update` IPC handler.

Only `courses.name/code/term` change. `folder_name` (the course's real files folder, and now the Phase 4 memory file — see below) is fixed at creation and never renamed — this was already true for the files folder before this session; the memory-file bug fix earlier today happened to make memory files follow the exact same rule for free, rather than needing separate rename-tracking logic. One visible consequence: a renamed course's memory file on disk keeps its original filename (e.g. `Old Name.md` after renaming to "New Name") — not a bug, same tradeoff the files folder already makes, but worth knowing if browsing `Atlas-Storage/course-profiles/` by hand.

Verified with a real scripted run (create a course, rename it via the actual UI, confirm the detail page/grid/memory file all behave correctly) plus a full `npm run verify` pass, since this is a genuine new feature touching several files, not a small tweak.

## Session 2026-07-29 (continued) — audited the finished Phase 4, found and fixed three real bugs

Before calling Phase 4 done, ran a battery of edge-case checks against the assembled system (duplicate course names, course-scoped search, an OCR-only scanned resource) rather than trusting that individually-tested pieces compose correctly. All three surfaced real, user-facing bugs:

1. **Memory files were keyed by course display name**, so two courses genuinely sharing a name (e.g. the same subject two terms running) silently shared one memory file — writing for one overwrote the other's, deleting either destroyed both. Fixed by keying on `folder_name` instead (unique, stable, already used for the files folder).
2. **`atlas_search`'s course/type filtering happened in JavaScript after SQL's `LIMIT`**, so a course-scoped search could return nothing even when real matches existed, if the globally top-ranked hits all belonged to other courses. Moved the filtering into the SQL query itself.
3. **`atlas_read_document` only read `origin = 'extracted'` parts**, so a scanned textbook the user ran OCR on would show up in search results but return zero content when the agent tried to actually read the page. Now falls back to OCR-derived parts per-resource.

All three are now covered by regression tests in `scripts/verify-mcp.js`, alongside a new check that an ambiguous course name returns candidates instead of silently picking one. Full `npm run verify` also re-run and passing.

## Session 2026-07-29 (continued) — Phase 4 Parts B–E: memory, agent notes, query layer, MCP server, export

Built straight through after Part A, per the user's go-ahead to keep going without pausing between parts, verifying at the end. Full detail in `phase4-spec.md`; this is the summary.

- **Part C — persistent memory as files**: `src/main/memoryFiles.ts`. Plain Markdown, one file per course plus one general (`_general.md`), in `Atlas-Storage/course-profiles/` — never shown in the Atlas UI, never parsed by Atlas. Seeded at course creation (all three creation paths: manual, Classroom mapping, Ashoka import), deleted alongside a course's files when the course itself is deleted (not archived).
- **Part B — agent-generated notes**: `notes.generated_by_agent` column. A 🤖 badge (orthogonal to the existing ✍️ handwritten badge — a note can in principle be neither, either, or both signal-wise, though not simultaneously in practice) plus a "Show only agent notes" filter on the Notes page.
- **Part D (query layer) — `src/main/contextBuilder.ts`**: the actual logic behind all 9 tools from the spec, as plain functions over a `better-sqlite3` `Database` instance with zero Electron dependency — shared verbatim by the MCP server (a separate process) and the static export (runs in-process). Course lookup accepts either a numeric id or a name (exact match, then unique substring; ambiguous names return candidates rather than guessing).
- **Part D (MCP server) — `src/main/mcpServer.ts`**: uses the official `@modelcontextprotocol/sdk`, stdio transport. Exposes exactly the 9 tools from the spec with the same response-size limits (search excerpts capped, `atlas_read_document` capped per-call). **Real bug found while building this, not guessed**: `contextBuilder.ts` was supposed to have zero Electron dependency, but transitively imported `paths.ts`, which calls `electron`'s `app.getPath()` — and `app` is `undefined` when the server runs under `ELECTRON_RUN_AS_NODE=1` (required so `better-sqlite3`'s native binding matches Electron's Node ABI; a plain `node` launch fails with a `NODE_MODULE_VERSION` mismatch, confirmed directly). Fixed by making `getDataDir()` fall back to the OS-default Downloads path when `app` isn't available. See `mcp-setup.md` for exactly how to point Claude Code/Codex/Cursor at it.
- **Part E — static export**: "Export for AI" button on course detail, writes a plain-Markdown dump to `Atlas-Storage/exports/` via the same `contextBuilder.ts` the MCP server uses, for AI tools that can't do MCP.
- **Verification**: new `npm run verify:mcp` (`scripts/verify-mcp.js`) drives the real MCP server over the real protocol via the SDK's own client — not a mock — against a seeded temp database, asserting all 9 tools' actual behavior (search returns excerpts not full text, an agent-created note is immediately searchable, the agent can create but never touch the user's own note, an unmatched course name returns an error rather than a guess). Full `npm run verify` (Playwright/Electron) also re-run and passing, since this touched course creation/deletion, notes, and search across several files.

## Session 2026-07-29 — Phase 4 Part A: page-aware text extraction

Built against `phase4-spec.md` §3 (see that file for the full design). Every PDF/PPTX/DOCX/XLSX resource now gets its text pulled into `document_parts` — one row per page/slide/sheet/section, each with a human-readable label ("Page 214", "Slide 12", "Sheet: Q3 Results") — instead of being searchable by filename only. This is the foundation the rest of Phase 4 (memory files, MCP server) builds on.

- New `src/main/textExtraction.ts`: per-format extraction (`pdfjs-dist`'s real text layer for PDF, the existing PPTX-outline regex repurposed to return raw per-slide text, `mammoth`+heading-split for DOCX, `xlsx` per-sheet for XLSX, fixed-size chunking for TXT/MD). Reuses a new shared `src/main/pdfjsLoader.ts` (the ESM dynamic-import workaround, previously duplicated only in `ocr.ts`).
- Runs automatically on every import (manual upload, folder watching, course-storage watching) via `scheduleExtraction()` in `main.ts`, in the background so a large textbook doesn't block the import. `resources.extraction_status` tracks `pending` → `done`/`empty`/`unsupported`/`failed`.
- **Backfill**: `extractAllPendingResources()` runs once at launch, catching every resource that predates this feature (all default to `pending` via the migration). No one-time flag needed — once processed, a resource's status is never `pending` again, so re-running on every launch naturally does nothing once caught up. Progress is shown live in Settings (`#extraction-status`) since a real library's first backfill could take minutes, and silent multi-minute background work is exactly how the Classroom sync bug went unnoticed for weeks.
- **OCR made page-aware**: `resources:saveOcrText` now splits the already-reviewed OCR text back into one `document_parts` row per page (`origin = 'ocr'`), instead of one blob. Safe because the review step is display-only (`textContent`, not editable), so the page separator `ocr.ts` joined with is guaranteed intact.
- The existing "Run OCR" button now shows a hint automatically when `extraction_status = 'empty'` — Atlas's own text extraction found nothing, which is the actual signal that a PDF is a scan.
- **Real bug caught by `npm run verify`, not guessed**: `document_parts`'s unique constraint was `(resource_id, ordinal)`, but a PDF can legitimately have both an `extracted` Page 1 (from a real text layer) and an `ocr` Page 1 (if OCR is run anyway) without colliding — fixed to `(resource_id, ordinal, origin)`.
- Verified extraction logic directly against real generated fixture files (a hand-built 2-page PDF, a minimal PPTX/DOCX/XLSX) before running the full suite — confirmed correct page/slide/sheet/section labels, and confirmed a genuinely text-free PDF page correctly reports `empty` (not `done` or `failed`).

## Session 2026-07-28 (continued) — fixed the Calendar-page refresh gap

Previously diagnosed but left unfixed across a context compaction (see prior session log below for the root-cause writeup). The actual fix landed in one place: `renderDeadlines()` in `renderer.ts` is the shared refresh point every deadline mutation already calls (save, reset-to-Classroom, delete, completion toggle), but it only ever refreshed Dashboard. Added `if (currentPage === 'calendar') void renderCalendarPage();` right alongside the existing `renderDashboard()` call there, so all four call sites get the Calendar refresh for free instead of needing four separate patches. Verified with `npm run build` and a full `npm run verify` pass.

**Revisit if it resurfaces**: the user separately said the reset action still felt like "no confirmation" — likely explained by this same bug (nothing was visibly moving because Calendar wasn't refreshing). Worth a fresh look if the complaint recurs now that the refresh gap is closed.

## Session 2026-07-28 (continued) — investigated a "Reset to Classroom version" bug report, fixed the real cause

The user reported the reset button felt buggy after trying it. Rather than guessing from the description, built a scripted end-to-end reproduction (real Electron app via Playwright, a simulated Classroom-linked deadline via direct DB writes) to actually verify what happens.

- **The reset mechanism was never broken** — confirmed the DB update, viewer refresh, and list refresh all worked correctly and immediately in the scripted test.
- **Real cause found**: saving an edited deadline closed the entire window with zero visible confirmation (pre-existing behavior, not part of this feature) — so whatever the user was looking at right after editing was already stale or gone, making "I clicked reset and nothing happened" an easy, reasonable conclusion. **Fixed**: saving now reopens the viewer showing the result directly.
- **User's follow-up ask**: make an edited deadline visible without having to open it. Added a `.deadline-edited-badge` shown in every deadline list (course-detail list/icon views, Dashboard's Upcoming widget, Calendar's upcoming sidebar), plus a compact "✎" prefix on Calendar month-grid chips (too small for the full badge).
- Verified with a second scripted repro confirming the badge appears without clicking in and disappears immediately on reset. `scripts/verify-app.js` updated (saving no longer self-closes, so 6 sites needed an explicit close added) — full suite passes.
- Docs: `open-questions.md` #3 updated with this follow-up.

## Session 2026-07-28 (continued) — configurable sync settings built (Phase 3 item 2 of 3)

- **Every source's sync schedule is now a real, visible, adjustable Settings control** instead of hardcoded/invisible — Off / On launch only / Every N minutes, per source (Drive, Classroom), stored as one `app_settings` string per source (`'off'`/`'launch'`/`'interval:<seconds>'`). Defaults reproduce the exact prior behavior (Drive 20s, Classroom launch-only), so nothing changes for the user unless they touch the new dropdowns.
- **"Last synced"/last-error is now visible per source** — this is what was missing when the Classroom adapter silently failed on every sync for weeks (session 46): there was simply no UI surface that could have shown it. Now there is.
- **Consolidated Classroom's old standalone "Sync now" button** (with its own `alert()`-based error popup) into the new unified per-source control, rather than leaving two differently-styled sync-now affordances for the same source.
- Verified with `npm run build` and a full `npm run verify` pass (no regressions) — real scheduling/timer behavior change, worth the full suite.
- Docs updated in the same pass: `open-questions.md` #2 resolved, new `ARCHITECTURE.md` §4 "Sync scheduling" subsection, `ROADMAP.md` checkbox ticked.
- **Phase 3's first two items (conflict handling, sync configuration) are now both done.** Per the agreed order, **Phase 4 (Context Builder + MCP) is next**, with the Gmail adapter deliberately last — see `open-questions.md` #28 for the full reasoning.

## Session 2026-07-28 (continued) — conflict handling built (Phase 3 item 1 of 3)

Built exactly as planned/confirmed with the user: per-field protection (not whole-row locking), a visible override marker + "Reset to Classroom version" action, and greyed-out-not-deleted for coursework removed at the source.

- **The bug this fixes was real and already live**: editing a Classroom-synced deadline (e.g. correcting a due date) was silently reverted on the very next sync, since `upsertDeadline` overwrote `title`/`due_at` unconditionally. Also, nothing ever reconciled an assignment deleted at the Classroom source — it just stayed in Atlas forever.
- `deadlines` gained `local_overrides` (which fields the user has edited since last sync), `classroom_title`/`classroom_due_at` (Classroom's current values, always kept current regardless of overrides — this is what makes "reset" instant and offline), and `classroom_removed`. `assignments` gained `classroom_removed` too, display-only (it has no direct editing UI — the user always edits the mirrored deadline instead).
- The actual per-field-preservation SQL (a `CASE`/`instr()` expression inside `ON CONFLICT DO UPDATE`) was verified directly against a real SQLite database before trusting it, not just read — confirmed an overridden field survives a re-sync while an untouched field still updates.
- Also fixed in the same pass: `assignments.posted_at` (added earlier this session) had never been added to `schema.sql`'s fresh-install table, only to the migration — a brand-new install would have been missing it.
- Verified with `npm run build`, a standalone SQLite logic test, and a full `npm run verify` pass (no regressions) — this one felt worth the full suite given it touches core deadline data integrity, not just UI.
- Docs updated in the same pass: `open-questions.md` #3 resolved, `ARCHITECTURE.md` §4b, `ROADMAP.md` checkbox ticked.
- **Next**: sync configuration (item 2 of 3), per the plan agreed this session.

## Session 2026-07-28 (continued) — planned the rest of Phase 3; Phase 4 moved ahead of Gmail

Planning only — **no code written this pass**, by explicit user instruction ("add or edit whatever you need to in the necessary docs and I will give you the go ahead and only then you get started"). The three remaining Phase 3 items were planned in detail and two roadmap-level decisions came out of it.

- **Found an active data-loss bug while researching, not yet fixed**: editing a Classroom-synced deadline is silently reverted on the next sync (`upsertDeadline` overwrites `title`/`due_at` unconditionally, and the deadline editor happily edits synced rows). Also, an assignment deleted in Classroom currently lingers in Atlas forever — there's no reconciliation pass for coursework at all. Both are now the concrete definition of the previously-vague "conflict handling" roadmap item. Full detail and the agreed fix in `open-questions.md` #3.
- **Conflict-handling policy decided**: per-field protection (edited fields are preserved, untouched fields still accept source updates), a visible marker + "reset to Classroom version" action, and greyed-out-not-deleted for items removed at the source. The user chose per-field over whole-row locking, and grey-out over hard delete, for stated reasons — see #3.
- **Sync configuration decided**: a real Settings section with per-source Off / launch-only / every-N-minutes, last-synced timestamps, last error, and per-source + global "Sync now". Defaults preserve today's behavior exactly. This finally answers `open-questions.md` #2's "manual, automatic, or configurable" — the answer is configurable, and the current invisibility is what let the Classroom adapter fail silently for weeks (#21).
- **Gmail adapter scoped, and deliberately deferred behind Phase 4** (`open-questions.md` #28). Scope: a user-managed tracked-sender list (~20 addresses, each optionally tied to a course), automatic tracking of threads the user replied to, full email readable in-app, attachments added to a course manually per attachment, a global Email page plus per-course tab, read-only access on its own connection. **No importance scoring anywhere in Atlas** — the user's core motivation ("the college spams us so much you miss something important") is a query-time judgment that belongs to the AI agent, not a stored fact, per `AGENTS.md`/PRD §17.
- **Phase 4 (Context Builder + MCP) now comes before the Gmail adapter.** The user's main want from Gmail can't be delivered without Phase 4; Phase 4 is the product's stated purpose and is still entirely unbuilt; and everything already in Atlas is worth querying today. User agreed. `ROADMAP.md` updated — working order is now: conflict handling → sync configuration → Phase 4 → Gmail.
- **Caveat the user raised twice and worth carrying forward**: their semester hasn't started, so they genuinely cannot yet say how many senders they'd track, or whether their professors will use Classroom or email. Any Gmail design decision that depends on real usage patterns should be treated as provisional until there's a real semester's mail to check it against.

## Session 2026-07-28 (continued) — Office file preview via Google Drive, reversing an earlier recommendation

Follow-up to `open-questions.md` #12, which had been sitting deferred since session 20 with a recommendation to hold off. The user pushed back directly on four points behind that recommendation, and three turned out to be wrong for their actual situation (not opposed to files leaving the machine for a better viewing experience; no Word/PowerPoint installed, so "use the desktop app" wasn't a real alternative; a text-only outline is such a low bar that almost any improvement clears it) — the fourth (Slides conversion fidelity) turned out to be moot once the plan changed from *converting* to Slides/Docs to just letting Drive preview the original Office file directly, which doesn't lose fidelity the way conversion does. Planned in detail before building, confirmed with the user, then built in one pass:

- **"Open in Google Drive"** — a new action (resource right-click menu, and a button in the in-app preview modal), for `.pptx`/`.docx`/`.xlsx` resources only. Uploads to a dedicated "Atlas Previews" folder Atlas creates and manages in the user's Drive, then opens Drive's real viewer in the browser. The existing local preview (text outline / mammoth HTML / SheetJS tables) is untouched and stays the default — this is additional, not a replacement, so offline viewing keeps working.
- **Upload is cached** (`resources.drive_preview_file_id` + the local file's size/mtime at last upload) — only the first open of an unchanged file re-uploads; a changed file re-uploads in place (same Drive file ID) rather than creating a duplicate.
- **OAuth scope added**: `drive.file` alongside Drive's existing `drive.readonly` — narrower than the existing grant (only touches files Atlas itself created), but per the standing scope-change gotcha, **the user needs to disconnect and reconnect Google Drive once** for it to take effect.
- Cleanup: deleting a resource or course also deletes its Drive preview copy (best-effort, non-blocking); a new "Clear Drive preview cache" Settings action wipes all of them at once.
- **Known, unfixed caveat**: the user has two Google accounts signed in (personal for Drive, college for Classroom) — opening a Drive link may land on the wrong account's "no access" page depending on which account the browser treats as active. Documented in `ARCHITECTURE.md` §7 as a real limitation, not silently ignored; not fixed in this pass.
- Verified via `npm run build`; the Drive round-trip itself isn't covered by `scripts/verify-app.js` (same live-account limitation as the rest of Drive/Classroom) — worth a manual check against real lecture decks once the user reconnects Drive.
- Docs updated in the same pass: `open-questions.md` #12 resolved, new `ARCHITECTURE.md` §7 subsection, `ROADMAP.md` Phase 3 item ticked.

## Session 2026-07-28 — real Dashboard/notes bugs found via an audit, plus course archiving built

The user asked for a review of which already-built features still had ambiguous or unfinished edges (ignoring anything not yet built). That review surfaced two real, previously-unreported bugs, which the user then asked to be fixed alongside two smaller items:

- **Classroom files falsely shown as "changed today"/"recently added" on every launch** — root-caused to `reconcileCourseStorage()` treating a Classroom attachment's external URL (`resources.file_path` for `kind = 'link'` rows) as if it were a file on disk, so every one of them got deleted and immediately recreated (with a fresh timestamp) on every single launch. Fixed, plus a related missing-uniqueness gap in the database that the delete/recreate cycle had been silently masking, plus a one-time automatic repair of the ~155 already-corrupted timestamps using Classroom's own real posting times. Full detail: `ARCHITECTURE.md` §4, `open-questions.md` #26.
- **Notes falsely marked as "changed" just from opening and closing them**, no edit needed — the note editor fires its own internal "content changed" signal once during setup, and closing always saved unconditionally. Both now check whether the content actually changed first, in two places (the editor itself and the underlying save handler) so any future save path inherits the same protection. See `open-questions.md` #26.
- **Dashboard's "View full calendar" button had grey empty space below it** — was floating directly under a height-capped list instead of anchored to the bottom of its box. Fixed with the same "pin to bottom" approach already proven on the Courses page's card layout (`open-questions.md` #25/#26) — one CSS line (`margin-top: auto`).
- **Course archiving built** (`open-questions.md` #4, previously open/deferred to Phase 5) — archive/unarchive a course without touching any of its files/notes/deadlines, via a right-click menu item or a button on the course's own detail page. The Courses page gained a "Show archived courses" toggle (active and archived courses are shown as two separate lists, never mixed). Archived courses stop auto-syncing from Classroom but stay fully searchable — archiving only declutters the course list, it doesn't hide material. `ROADMAP.md` Phase 5's archiving item is now checked off.
- Two items surfaced in the same review were explicitly deferred by the user for a separate discussion later, not built in this pass: revisiting PPTX/DOCX/XLSX preview fidelity (`open-questions.md` #12), and the `xlsx` package's known security advisories (accepted risk, `ARCHITECTURE.md` §7) — no action taken on either.
- Verified with `npm run build` after each change; no full `npm run verify` run this session — per the user's standing instruction, that suite is reserved for major changes, and while the course-archiving feature is real feature work, the individual pieces were each verified by build + the fixes are logic-level, not UI flows `verify-app.js` would meaningfully exercise better than a manual look. Worth a `npm run verify` pass and/or a manual look at the real `Atlas-Storage` data before considering this fully closed out.

## Handoff note — this is now the active working directory

The user is moving to a brand-new chat session with **`Atlas-v2` (this directory) as the working directory** going forward — the tool/agent driving that session isn't fixed, which is exactly why the project was generalized to `AGENTS.md` in the first place (see the fork entry directly below). A fresh session should:

- Read `AGENTS.md` → this file → `open-questions.md`, in that order, before doing anything (per `AGENTS.md`'s own onboarding instruction).
- Not assume anything about `C:\Users\Pamac\Downloads\Atlas` (the original repo) — that directory is explicitly Claude-Code-only territory and out of scope here; this fork is now the one being actively developed.
- Know the repo-local git identity here is currently still `Claude <noreply@anthropic.com>` (set when this fork was created and pushed) — if a different tool is doing the committing now, update it per `AGENTS.md`'s "Identity in this repo" section rather than leaving a misleading author on new commits.
- The GitHub remote for this fork is `PamacKR/Atlas-v2` (private), separate from `PamacKR/Atlas`.

## Session — sidebar collapse button missing on Resources/Calendar (real bug, not missing UI)

There's only one "Collapse" control in the whole app — the sidebar's own collapse toggle (`#sidebar-collapse-toggle`), pinned to the bottom of the sidebar nav via `margin-top: auto`. It isn't a per-page element, so "missing on Resources/Calendar" couldn't be a missing-markup issue — it turned out to be a layout bug that happened to only manifest on those two pages:

- `#app-shell` was `min-height: 100vh` with no `overflow` set, so the whole document scrolled as one unit. Since `#sidebar` is a flex sibling of `#main-area` with the default `align-items: stretch`, its height matched whatever `#main-area`'s content needed — on a page taller than the viewport (Resources with many items, or the Calendar month grid), `#sidebar` itself grew taller than the screen too, pushing its bottom-pinned Collapse button below the visible viewport. It wasn't missing, just scrolled out of view on exactly the two pages whose content is tallest.
- Fixed by making `#app-shell` a fixed `height: 100vh` with `overflow: hidden`, and giving `#main-area` its own `overflow-y: auto` — so tall page content scrolls *inside* the content area, `#sidebar` stays pinned at exactly the viewport height on every page, and the Collapse button is always visible regardless of how tall the current page's content is.

## Atlas-v2 fork — project generalized away from being Claude-Code-specific

This repo is a clone of `PamacKR/Atlas`, forked 2026-07-27 so the user could try a different AI coding agent/tool against the project without touching the original repo, which stays Claude-Code-only. Forking it surfaced a real gap: the project's own operating instructions and several docs assumed Claude Code specifically, rather than describing a model/tool-agnostic contract — so this fork also generalizes that, as a one-time change made only here (not backported to the original `Atlas`, which the user wants left alone).

- **`CLAUDE.md` trimmed to a 5-line stub**, pointing to a new **`AGENTS.md`** — the real, full operating doc going forward, adapted line-for-line from `CLAUDE.md`'s content with "Claude"/"Claude Code" generalized to "the AI agent"/"whichever AI coding tool is being used." All the substantive rules carry over unchanged (both hard guardrails, the doc map, both recurring-CSS-bug writeups, the git-workflow autonomy grant, the `npm run verify` self-testing instruction). The one policy reworded rather than just renamed: git identity — instead of mandating one literal string, `AGENTS.md` states the principle (a clearly-attributed, tool-specific local git identity per clone) so a different tool's session and a Claude Code session can each use their own without contradicting the doc.
- `ARCHITECTURE.md` §6 ("Claude Code integration: local MCP server") renamed "Runtime AI integration: local MCP server" and reworded — MCP is a vendor-neutral protocol, not tied to one client; the ASCII layer diagram's `Claude Code (reasoning)` box now reads `AI agent (reasoning)`. `ROADMAP.md` Phase 4's heading/bullets reworded the same way. `README.md`'s intro and doc links updated to point at `AGENTS.md` and describe "external AI coding agents" generally. `open-questions.md` #5 (AI provider independence) reworded to drop the "initial target is Claude Code" framing, plus a couple of forward-referencing `` `CLAUDE.md` `` citations elsewhere in that file repointed to `` `AGENTS.md` ``.
- A handful of source comments (`renderer.ts`, `main.ts`, a few in `styles.css`/`schema.sql`) that pointed back to "CLAUDE.md" as the doc-of-record were repointed to `AGENTS.md`; no logic changed anywhere in this pass.
- **Not touched**: `prd.md` (the user's original source document — off-limits per the standing rule, carried into `AGENTS.md` unchanged) still uses "Claude Code"/"Claude" throughout its own sections (18, 20, 21); those are historical/original text, conceptually superseded by the generalized framing elsewhere but not edited. Every existing dated entry below this one is left exactly as originally written — they're an accurate record of what was true in each session, not retroactively rewritten to match the new tool-agnostic language.
- Phase 4 (Context Builder/MCP server) is still **unbuilt** — this was a documentation-only pass, no functional code changed.

## Session 46 (continued yet again) — Dashboard stretch/equal-width polish, theme toggle moved to Settings-only, real "upcoming deadlines" fix

Six more user-reported issues against the just-restructured Dashboard, all from the same shared reference screenshot the user's been using throughout this session:

- **Equal-height columns, flat bottom**: `#dashboard-main-grid` had `align-items: start` (added in the previous entry specifically to work around the `flex-direction` row bug). With that bug now actually fixed, `align-items: start` was overcorrecting — the user wants the three columns' bottoms flush, matching the reference. Reverted to the Grid default (`stretch`, so the property is just omitted now) and made "My courses"/"Upcoming" genuinely fill their stretched cell height via a new `.dashboard-widget-stretch` (flex column) with the inner `<ul>` set to `flex: 1`, so the footer button sits pinned at the bottom rather than floating right under whatever height the list content happens to need.
- **Equal-width course rows**: "My courses" used wrapping chip cards (`flex-wrap`), so a short course name produced a visibly narrower card than a long one. Replaced with a flat, full-width divided list (one row per course, thin `border-top` divider, no per-row border/background) — same idiom the rest of the dashboard's list widgets already use.
- **Theme toggle removed from the top bar** — `#theme-toggle` deleted from `index.html`/`renderer.ts`/`styles.css` entirely. Theme switching is Settings > General only now (`#settings-theme-dark`/`#settings-theme-light`, unchanged). `scripts/verify-app.js`'s theme test updated to navigate to Settings and click those buttons instead of the removed top-bar button.
- **"Manage courses" / "View full calendar" standardized**: both were styled differently (a plain header button vs. a `.link-button`) and sat in their widget's header. Both are now the same shared `.dashboard-widget-footer-btn` — full-width, pinned to the bottom of their widget via the flex-stretch layout above — rather than one being a header action and the other a footer link.
- **Real bug: "upcoming deadlines" counted stale imports as upcoming.** The stat tile and the Dashboard Upcoming widget had no time filter at all (`dashboard:stats`/`dashboard:upcomingDeadlines` in `main.ts`) — every incomplete deadline with a due date counted, including ones months overdue from the old Classroom import. Per the user's own framing ("only show them past due if, when added, they still had a due date in the future"), the fix is a new `deadlines.stale_import` column (`src/main/db/schema.sql`, migrated in `database.ts` with a one-time backfill flagging any deadline already overdue at migration time), set at insert time — never touched by `ON CONFLICT ... DO UPDATE`, so re-syncing doesn't retroactively unflag anything. `googleClassroom.ts`'s `upsertDeadline` and `main.ts`'s `deadlines:create` both compute it (`due_at` already in the past *at insert time* → stale). `dashboard:stats`/`dashboard:upcomingDeadlines` now filter `stale_import = 0`; `deadlines:listAllWithCourse` (the full Calendar page) is deliberately untouched, so past deadlines still show there as the user asked. A deadline that was future when created and has since become genuinely overdue keeps `stale_import = 0` and still surfaces as "Overdue" in Upcoming — only already-dead-on-arrival rows are hidden.
- Settings-page expansion (item 6 of this batch) explicitly deferred — user said they'd give more detail on it separately.

## Session 46 (continued again) — real Dashboard layout bug, not a design choice

The user pointed out the Dashboard's "Upcoming" widget looked badly broken (rows stacked vertically and centered, huge dead vertical space) and that "Recently added"/"What changed today" had large empty space below their content. Both were real CSS bugs from the widget-reformat work earlier this session, not intentional:

- **The actual bug**: `#dashboard-deadlines li.upcoming-row` set `display: flex` but never set `flex-direction`, so the generic `.dashboard-widget ul li { flex-direction: column }` rule won the cascade for that one property (CSS resolves conflicting declarations per-property, not per-rule) — every row rendered as a centered column instead of a horizontal row. Fixed by adding `flex-direction: row` explicitly, the same fix already correctly applied to the course-chips widget elsewhere in the file (a pattern that should have been copied, not re-derived).
- **The empty-space bug was a side effect of the first one**: `#dashboard-widgets` was a CSS Grid, which stretches every column to match the tallest one by default (`align-items: stretch`) — the "Upcoming" widget's bug made it artificially tall, so its shorter siblings stretched to match and showed dead space below their real content. Fixing the row bug shrank "Upcoming" back down, and adding `align-items: start` to the grid stops this from ever recurring for a genuinely different-height column.
- **Also acted on**: "follow this kind of layout for the dashboard" (a third shared screenshot) — restructured the Dashboard into the same three-column arrangement (My Courses | Upcoming | Recently added + What changed stacked) instead of My Courses full-width above a separate row of three widgets. New `#dashboard-main-grid`/`#dashboard-right-column` in place of the old `#dashboard-widgets`.

## Session 46 (continued) — UI polish batch: tabs, Settings page, Escape-to-close, clickable Classroom content

A follow-up batch of 8 user-requested items after the Classroom bug-fixing above, done in one pass, verified with `npm run build` after each chunk and a single full `npm run verify` at the end (per the user's instruction not to re-run the whole suite after every edit).

- **Link-resource icons**: a `kind='link'` resource showed a generic 🔗 for everything. `resourceDisplayIcon()` now guesses a real PDF/DOCX/PPTX/etc icon from the linked file's own name/extension (display-only — the stored `kind` stays `'link'`, that's what drives the external-open behavior).
- **Global Escape-to-close**: every remaining overlay/modal (Classroom connect picker, review panels, deadline editor, course picker, the new Classroom item detail modal, the confirm dialog) now closes on Escape, not just the note editor/resource preview that already had it. Confirm-overlay's Escape resolves as Cancel.
- **Clickable Announcements/Assignments/Classwork rows**: clicking an assignment opens its mirrored deadline (real editing, not just a read-only view) via a lookup by `classroom_coursework_id`; clicking an announcement/classwork item opens a new shared read-only `#classroom-item-detail-overlay`.
- **Dashboard "Upcoming" widget reformat**: three-line stacked date block (month/day/weekday), right-aligned "Due in" block with a red "urgent" color when 2 days or less away — matching the shared screenshot more closely than the original two-line version.
- **Calendar day-cell chips enlarged**: two-line (title + course name), background-tinted with the course color (not just a thin left border), row height increased — max chips per day dropped from 3 to 2 since the taller chips no longer fit 3.
- **Course detail page rebuilt as tabs** (Overview/Deadlines/Announcements/Assignments/Classwork/Files) — replaces the old single long-scroll page, the user's main complaint ("too much scrolling"). The Classroom-specific tabs only appear once a course is actually linked; `selectCourse()` always resets to Overview for a freshly-opened course. `scripts/verify-app.js` updated to click the right tab before interacting with Deadlines/Files content (`selectCourse()` resets to Overview every time the course is reselected, so several spots needed a tab click re-added).
- **New Settings page** (sidebar entry) — Google Drive/Classroom connect widgets moved here wholesale from the Dashboard (`Settings > Sources`, same element IDs, no duplication), plus a real `Settings > General` with a Dark/Light theme picker (mirrors the existing top-bar toggle, both drive the same persisted `theme` setting) and 5 accent-color swatches that override `--color-accent` (persisted as `accentColor`) — since that one CSS variable already drives active states/buttons/highlights app-wide, picking a swatch recolors all of them at once. `Settings > About` shows the app version via a new `app:getVersion` IPC call.
- Docs updated in the same pass: `open-questions.md` #22 (new), `ARCHITECTURE.md`/`ROADMAP.md` touched where relevant.

## Session 46 — Classroom sync fix, Drive-link attachments, Classwork, Connect button, Dashboard/Calendar

Executed the plan approved at the end of session 45 (see `open-questions.md` #21 for the full writeup). Built in one pass, verified with `npm run build` after each chunk rather than a full `npm run verify` run per change (per the user's explicit instruction this session not to re-run the whole test suite after every edit) — one `npm run verify` pass still happens before the final commit.

- **Fixed the real bug reported at the end of session 45**: a Classroom sync that pulled zero content for every mapped course. First pass added a per-course `try`/`catch` (a real improvement, kept), but the user then tested "Connect to Classroom" live and hit the exact same class of failure via an error dialog: `"ON CONFLICT clause does not match any PRIMARY KEY or UNIQUE constraint."` Root-caused (not guessed) by preparing the actual SQL directly against the real `atlas.db`: `upsertAssignment`/`upsertDeadline`/`upsertAnnouncement` targeted a *partial* unique index but put the `WHERE ... IS NOT NULL` predicate after `DO UPDATE SET` instead of right after the conflict column list — SQLite silently refuses to recognize a partial index as a valid conflict target unless the predicate is restated in the right spot, so these three `db.prepare()` calls threw unconditionally, every time, before the sync loop even ran. This was very likely the *actual* cause of every "nothing synced" report since the adapter was first built — fixed by moving the `WHERE` clause to the correct position on all three. Re-verified by preparing both versions against the live database before rebuilding.
- Attachments now land as Drive-link `resources` (`kind='link'`, `file_path` = the external URL, opened via `shell.openExternal`) instead of downloaded files — the user's explicit preference, and it incidentally covers link/YouTube/Form materials that previously had nothing to download at all.
- Added Classwork (`courseWorkMaterials`) sync into a new `classwork_materials` table.
- Added a per-course "Connect to Classroom" button on course detail — the workflow the user actually wanted (pre-create courses via Ashoka import, then connect directly) rather than only linking through the discover-new-courses review panel.
- Added inline Announcements/Assignments/Classwork sections to course detail (shown only once linked).
- Redesigned the Dashboard's "Upcoming" widget (kind-filter tabs, date block, course color, relative due-in label) and built a new Calendar page (month grid + Upcoming sidebar + course legend) from the two screenshots the user shared. Day/Week toggle, a date-picker, and a Filters panel are logged as fast-follows in `open-questions.md` #21, not silently cut.
- Docs updated in the same pass: `ARCHITECTURE.md` §4b (rewritten bullets) and new §4d, `open-questions.md` #21 (new), `ROADMAP.md` Phase 3 bullets.
- **Third live-test bug, found right after the SQL fix**: connecting a course got past that error but then failed with `"Request had insufficient authentication scopes."` — the newly-added Classwork sync (`courseWorkMaterials.list`) needs `classroom.courseworkmaterials.readonly`, which was never added to `googleAuth.ts`'s `CLASSROOM_SCOPES` list. Fixed. **Action needed from the user**: disconnect and reconnect Google Classroom once — the existing refresh token was issued under the old scope set and won't gain the new scope on its own.
- **Product gap the user caught before testing further**: asked what happens to already-synced posts if they disconnect a course they'd connected to the wrong Classroom class — turned out disconnecting only cleared the link, content stayed, so there was no real undo for a bad mapping. User's explicit choice: made per-course "Disconnect" destructive — it now also deletes that course's synced announcements/assignments/deadlines/classwork/link-resources (one `db.transaction()`), confirmed first via `showConfirm` since it's irreversible. The Dashboard's global Classroom "Disconnect" (revokes the OAuth token) is untouched.

## Where things stand (accurate as of session 44 — read this before the per-session log below)

- Repo: [github.com/PamacKR/Atlas](https://github.com/PamacKR/Atlas), private, owned by the user (`PamacKR`). Commits authored as `Claude <noreply@anthropic.com>` (repo-local git config) — don't change without being asked.
- **Phase 0 (docs) done. Phase 1 (local-only workflow) is substantially built and working**, not just scaffolding:
  - Courses: create (name/code/term-dropdown), list, delete (right-click → native menu → in-app confirm modal). Each course gets a human-readable on-disk folder (`courses.folder_name`, sanitized + collision-safe, computed once at creation).
  - Resources: manual upload (native file picker), list per course, delete (same right-click pattern), original filenames preserved on disk.
  - **In-app preview** for every supported kind: PDF/image render natively, markdown via `marked` (pinned to v12 for CJS compat), DOCX via `mammoth`, XLSX via `xlsx`/SheetJS (HTML tables, formula evaluation off), PPTX as a text-only slide outline (documented scope line — no free library renders real slide layout).
  - **Open in browser** (session 20, replaces the old "Open in default app"): right-click any resource → opens it in the user's real default browser, in a real tab, instead of Electron's own preview or a native desktop app. Backed by a loopback-only local HTTP server (`src/main/localServer.ts`) that streams PDF/image/text as their native type and reuses the exact same markdown/DOCX/PPTX/XLSX conversion the in-app preview uses for everything else. Deliberately does not route through Google Docs/Sheets — that would require uploading files to Google Drive first (a real Phase-3-scale architecture change), whereas this keeps everything local. See `ARCHITECTURE.md` §7.
  - Image previews: center on both axes, zoom via buttons/Ctrl+scroll (25–400%), **remembered per-resource** (`resources.zoom_level`). Zoom is strictly scoped to images only — this took three rounds to fully close out, see sessions 15/17/18 below and the `CLAUDE.md` "recurring CSS bug" note.
  - List/Icon view toggle for the resource list.
  - Fullscreen toggle on the preview panel (icon buttons, tightened chrome).
  - **Local folder watching** (session 19, deletion sync added session 21): per-course "Watched folders" section — user picks a folder via a native directory picker, explicitly mapped to that one course (`watched_folders` table, `chokidar`). New files (including ones already sitting in the folder when watching starts) are copied into the course's managed storage automatically, identical to manual upload, and the open resource list refreshes live. Deleting a file from a watched folder deletes the resource it produced too (live via `unlink`, retroactively via reconciliation on watcher start). Folder→course mapping is deliberately explicit, never auto-guessed — see `open-questions.md` #11 and `ARCHITECTURE.md` §4. "Stop watching" is right-click-only on the folder entry, same pattern as course/resource delete.
  - **Every course's own managed storage folder is watched too** (session 22): separately from opt-in watched folders, `Downloads/Atlas-Storage/files/<course>/` itself is watched — a file dropped in there by hand is auto-imported, and one deleted from there is auto-removed from the app (same live + startup-reconciliation pattern). This makes the "user-browsable, hand-editable" data directory design (`ARCHITECTURE.md` §2) actually hold; previously only external watched folders were monitored, so hand-editing Atlas's own storage silently desynced from the DB (see session 22 below for the real bug this fixed).
  - **App-wide settings**: new `app_settings` table for preferences that shouldn't reset per launch. Resource list/icon view mode (session 22) and semester filter (session 23) — both global/persisted, restored on every launch.
  - **Notes (session 23, editor replaced session 24, polish sessions 25–28)**: flat per-course note list (no folders — title your own sessions, e.g. "W1L1"), Markdown storage, live-rendering WYSIWYG editor matching the Notion/Obsidian feel, autosave 600ms after the last edit, list/icon view toggle shared with resources. Right-click → "Open in browser" (read-only, math rendered via KaTeX) or Delete. Editor is **Milkdown (`@milkdown/crepe`)**, not Toast UI Editor — switched after the user reported Toast UI felt "raw and unpolished": no live `- `/`1. `/`---` shortcuts and no toolbar active-state feedback, both confirmed as real gaps through hands-on testing. Sans-serif fonts throughout. **Title auto-follows the first line of the note, Google-Docs style** (inline formatting like `**bold**` stripped too, and an image-only first line is skipped rather than turned into a garbage title), until the user manually renames it, which permanently decouples the two. **Headings are bold by default and Ctrl+B still toggles it off** — a real ProseMirror-level customization, not just CSS (see `ARCHITECTURE.md` §8). **Images embedded in notes are saved per-course, to `Atlas-Storage/files/<course>/notes/note-images/`** (session 29 — previously one shared flat `Atlas-Storage/note-images/` folder across all courses; the user disliked that and asked for images to live under the course folder like everything else) and served by the local HTTP server (fixed port `47823`, URL now carries the course id: `/note-image/:courseId/:filename`). Slash-menu heading labels are "H1"–"H6". **Every note is also one-way-exported to a plain `.md` file** at `files/<course>/notes/<title>.md` — the DB stays the real editing source, but this makes a note actually retrievable/usable outside Atlas. **Embedded images in the export are linked via a relative path back to that course's own `notes/note-images/` file, not copied** (session 28 — a copy was the original approach, but the user noticed the same image existing twice on disk and asked for no duplication; session 29 moved the shared store to per-course, simplifying the link from `../../../note-images/...` to `note-images/...`). See `open-questions.md` #1 (resolved) and `ARCHITECTURE.md` §8.
  - **Semester filter (session 23)**: course list can be scoped to one term at a time via a dropdown; persisted app-wide. Course-creation dropdown now says "Semester" (was "Term (optional)"), matching the filter's wording (session 26).
  - **Renderer now has a build step** (session 23): `esbuild` bundles `src/renderer/renderer.ts` (needed to use the notes editor's npm package, which isn't usable from a bare `<script>` tag) — see `ARCHITECTURE.md` §1 "Renderer build pipeline." `main.ts`/`preload.ts` still compile via plain `tsc`, unaffected.
  - **Global search (session 30)**: one search box in the header, queries across every course's notes and resources at once (not scoped to whatever course is open). Backed by the existing FTS5 `search_index` table, which is now actually kept populated — full-rebuilt on every resource/note create/update/delete and once at app startup, rather than incrementally maintained at each of the many call sites that can change one (see `ARCHITECTURE.md` §9 for why full-rebuild was chosen over incremental sync). Clicking a result jumps straight to it — selects the course, then opens the matching note or resource in the same editor/preview UI used everywhere else. Debounced 250ms, shows an explicit "No matches" state, matched terms highlighted in results.
  - **Deadlines (session 31)**: per-course "Deadlines" section — title, kind (assignment/reading/quiz/lab/project/exam/manual), optional due date **and optional time**, optional **description supporting `@`-mention references** to that course's resources/notes (clickable, same convention as Claude Code's own `@file` references), complete/incomplete checkbox, edit (reuses the add modal in a read-only "view" mode with an Edit button), delete via the usual right-click pattern. Sorted incomplete-first, then soonest-due-first, no-due-date items last. Due dates can be **typed as `dd-mm-yyyy` or picked from a calendar** — both, not one or the other. Near-term due dates show **"Today"/"Tomorrow"** instead of a date. Shares the app-wide list/icon view toggle with resources/notes, each `kind` getting its own icon. Asked the user whether to also build a separate "Assignments" tab (schema has a distinct `assignments` table with description/submission-status, meant for Phase 3 Classroom sync data) given the two overlap for manual entry — they chose Deadlines only for now; see `open-questions.md` #13 and `ARCHITECTURE.md` §10.
  - **Menu bar hidden by default (session 31)**: `autoHideMenuBar: true`, Alt reveals it — per user request to save screen space.
  - **Dashboard v1 (session 32)**: global overview above the course list (not scoped to whichever course is open) — three widgets, exactly what `ROADMAP.md` scoped for v1: **upcoming deadlines** (incomplete, has a due date, across every course, soonest first), **recently added resources** (across every course), and **what changed today** (resources added + notes updated, filtered to the user's local calendar day). Clicking any item selects its course and opens it directly in the same preview/editor/viewer UI used everywhere else. Refreshes opportunistically whenever `renderResources()`/`renderNotes()`/`renderDeadlines()` run (which is after every mutation already), rather than a dedicated hook per mutation site. See `ARCHITECTURE.md` §11.
  - **Phase 1 is now fully complete** per `ROADMAP.md`'s checklist (session 37 closed out the one remaining item, "Course workspace UI," ticked off — the course detail page already covers Overview/Resources/Notes/Deadlines/Files, Assignments/Announcements deliberately deferred to Phase 3 per `open-questions.md` #13). Unified Resource Library is also checked off — manual upload and folder watching already share one per-course resource list regardless of source.
  - **Phase 2 (handwritten notes / OCR) is now complete too** (session 39, `ROADMAP.md` fully checked off). "+ Import scan" on the Notes page imports a photo or (the real-world primary case, confirmed with the user) a multi-page PDF scan — one imported file always becomes one note, however many pages, via `src/main/ocr.ts` (Tesseract.js for OCR, `pdfjs-dist`/`@napi-rs/canvas` to rasterize PDF pages first). Genuinely local/offline: English language data and pdfjs's standard fonts are vendored in-repo (`assets/tessdata/`, `assets/standard_fonts/`) rather than left to their libraries' CDN defaults. `notes.image_path`/`notes.ocr_text` (already in the schema ahead of this feature) hold the original scan and raw extraction; `content_markdown` is seeded from the OCR text so autosave/search/editing already work with zero special-casing — a ✍️ badge and a "View original scan" toggle (reusing the Resources preview's exact rendering) are the only visible differences from a typed note. Opening a handwritten note now shows the original scan by default (session 41) rather than a blank editor. See `ARCHITECTURE.md` §3.
  - **On-demand OCR for regular Resources PDFs (session 41)**: a "Run OCR" action on the Resources preview modal, for typed/printed PDFs with no text layer (e.g. a scanned book) — the case the user raised in session 40 but wasn't built yet. Same opt-in/reviewed shape as the Notes OCR flow: extracted text is shown for review, and only explicitly saving it writes to a new `resources.ocr_text` column and makes it searchable. The original PDF still opens by default; Run OCR is a strictly additional action. See `ARCHITECTURE.md` §3.
  - **Phase 3 (external sync) is underway — Google Drive adapter built first (session 43)**, reordered ahead of Classroom/Gmail since it directly replaces the user's actual daily pain point (manually relaying phone scans through a Discord self-chat). Bring-your-own Google Cloud OAuth client (personal account, loopback-redirect flow, `src/main/googleAuth.ts`), a single designated Drive "inbox" folder scanned recursively (subfolders included) on every launch plus a 20s poll while Atlas is open, and a review panel (Dashboard → "Review new files") where the user assigns course + Resource/Note per file — individually, in bulk, or explicitly **Ignore**d — before anything is downloaded into local managed storage. See `ARCHITECTURE.md` §4a and `open-questions.md` #19 for full detail, including the accepted limitations (no native Google Docs, no cross-upload dedup, no live two-way sync).
  - **Google Classroom adapter built (session 44)**, right after Drive — the next Phase 3 item on `ROADMAP.md`. Separate OAuth connection from Drive's (`googleAuth.ts` gained parallel Classroom scopes/token plumbing, expected to be the college Workspace account), no background polling — launch scan + an explicit "Sync now" button only. New Classroom courses surface in a course-mapping review panel (Dashboard → "Review new courses", `#classroom-review-overlay`, same CSS Grid alignment pattern as Drive's review panel) with a name-match suggestion pre-filled but always requiring the user to confirm map-to-existing-or-create-new. Once a course is mapped, its coursework/announcements/Drive-file attachments sync automatically (no per-item review, unlike Drive — the ambiguity a review step resolves doesn't exist once Classroom's own typed, course-scoped data is what's being imported) into `assignments`/`announcements`/`resources`, and assignments also mirror into the existing `deadlines` Dashboard timeline so they're visible without a new UI screen. Announcements have no UI surface yet (no due date to mirror) — logged as a fast-follow, not silently dropped. See `ARCHITECTURE.md` §4b and `open-questions.md` #20 for full detail including the attachments/coursework-auto-import judgment calls.
  - **UI reskin (sessions 33–37, several rounds of hands-on feedback)**: real sidebar page routing (Dashboard/Courses/Resources/Notes, `showPage()`, no Settings page — added then removed once the user noted there's nothing to configure yet), light/dark theme toggle (persisted, genuinely applies to the Milkdown note editor too), colored initial-letter course avatars, and global cross-course Resources/Notes pages — inspired by mockups the user shared, reworked more than once after direct feedback that earlier passes were too shallow or had real usability problems. Current state (session 37, see `ARCHITECTURE.md` §12 for full detail): Courses page shows only the course grid, clicking a course opens a genuinely separate detail view (not an inline drill-down) with a real "← Back to Courses" **button** (styled like the app's other secondary buttons, not bare text), inline previews of the course's own Resources/Notes (a handful of items each, right-click → "Open in browser" wired the same as the main Resources list, "View all" **styled as a real button** for the rest); both the Resources preview and the Notes editor are **global overlay modals**, not scoped to their own page — opening either one from Dashboard, a course detail view, search, or a deadline mention never navigates away from whatever page is currently showing, it just layers on top (session 37 moved the Notes editor from a docked pane to `#note-overlay` specifically so this held for notes too, matching what the Resources preview already did); both have the same two fullscreen levels — "Expand width" (`#note-widen`/`#preview`-equivalent, a `↔` icon, widens the overlay panel) and genuine "Fullscreen" (`#note-fullscreen`, the same corner-expand icon used by the Resources preview's fullscreen button, covers the whole window) — in that order, then Close; the Resources list view pushes the file-kind tag to the far right instead of crowding it next to the course name; **Dashboard deadlines/resources/"what changed today" open in place on left-click, and right-click gives a "Go to" action** (a small custom menu, not native) that navigates to where the item actually lives — course detail for a deadline, the filtered Resources/Notes page for a resource/note — and opens/previews it there too, i.e. exactly the original click behavior, just moved to right-click now that left-click means "stay here" (session 37); uploading/creating opens a real course-picker modal with search, replacing an anchored popup that cut off past the screen edge with more than a couple of courses — Upload's modal also supports drag-and-drop (as does dropping a file directly onto the Resources page); Grid/List is the one naming convention and button order everywhere (Courses/Resources/Deadlines); Resources gained a sort dropdown positioned like Courses' own; the sidebar can collapse to icons; window size/position persists across launches; the Dashboard has a stat strip plus "My courses" as its own chip-card row; Electron's default application menu (the odd-looking one Alt used to reveal) is removed entirely. Scope stayed bounded throughout to a reskin/restructure of what already exists — no Calendar/Tags/Trash/Notifications, no separate Assignments concept (per the earlier decision in `open-questions.md` #13).
- **Self-testing:** `npm run verify` (`scripts/verify-app.js`) launches the real app via Playwright's Electron driver and exercises courses, upload, preview (markdown/image/txt), zoom + persistence, delete (via direct API call, since native context menus can't be automated), view toggle, folder watching (pre-existing files in a newly-watched folder + a file dropped in live, both auto-imported), and handwritten-note OCR import (a checked-in 2-page PDF fixture via the native-dialog test hook, plus a synthetic drag-and-dropped image, both run through the real Tesseract/pdfjs pipeline, not mocked) — all against a throwaway temp dir, never real storage. Extend this file whenever a UI change needs verifying, rather than relying solely on the user.
- **One-click launch:** `Launch Atlas.bat` (repo root) + a `Atlas` Desktop shortcut pointing to it. Both run `npm start` (`npm run build && electron .`), so every launch always rebuilds from current source — no separate deploy step exists or is needed.
- **User's standing test data** (real `Downloads/Atlas-Storage`, not test-suite temp dirs) — **replaced in session 44**: the original two seeded courses (Data Structures & Algorithms/CS201, Database Systems/CS305) were deleted at the user's request once real data existed to replace them with. Now: the 5 courses actually imported via the Ashoka Planner import (Development Economics/ECO-3500-2, Economics of Gender/ECO-3660-1, The Theory of Corporate Finance and Banking/FIN-3555 FIN-4555-1, Technology Law and Policy/ENT-2032 POL-2120 IR-2070-1, Behavioral Finance/FIN-3563 ECO-3563 FIN-4563 ECO-4563-2 — all `term = 'Monsoon'`, ids 12–16), each now with 8 placeholder resources covering every kind Atlas can preview — `syllabus.txt`, `week1-notes.md`, `midterm-questions.pdf`, `assignment.docx`, `lecture-slides.pptx`, `gradebook.xlsx`, `reference-diagram.png`, `extra-materials.zip` — all genuinely valid files of their format (hand-built minimal PDF/OOXML zips, a real `xlsx`-library workbook, a real `@napi-rs/canvas` PNG), not renamed `.txt` files, added directly via one-off scripts (not through the app UI) at the user's request for preview-testing variety. Each course also has 3 upcoming deadlines with varied kinds (reading/assignment/quiz/exam/project/lab) and due dates spread across the next several weeks (some date-only, some with a time), also added via script. Leave alone unless told otherwise.
- Recurring lesson (hit 3x — `#preview-overlay`, `#confirm-overlay`, `#zoom-controls`): an element toggled via `el.hidden` must never have `display` set unconditionally on its own ID selector, or that CSS outranks the default `[hidden] { display: none }` rule. Written into `CLAUDE.md` as a standing check.

## Decisions locked in (don't re-litigate)

- **Stack:** Electron + TypeScript, SQLite (`better-sqlite3`, FTS5 for search). See `ARCHITECTURE.md` §1–2.
- **OCR:** local/offline via Tesseract.js — explicitly *not* a cloud OCR API, because of the zero-API-fees constraint. Handwriting accuracy will be mediocre; that's an accepted tradeoff, not a bug. See `ARCHITECTURE.md` §3.
- **Claude integration:** local MCP server exposing a Context Builder query layer; static file export kept as a fallback for non-MCP tools. See `ARCHITECTURE.md` §6.
- **Data directory:** `Downloads/Atlas-Storage/` — an Atlas-managed folder, deliberately placed somewhere browsable rather than a hidden system path, so the user can manually add/remove files, and deliberately a *different* folder from this repo (`Downloads/Atlas/`) so git operations can never touch real data. Course subfolders under `files/` are named after the course itself, not an ID. DB and config live alongside it. See `ARCHITECTURE.md` §2 and `open-questions.md` #6.
- **Google Drive adapter:** bring-your-own personal-account OAuth client (not the college Workspace account — no reason to route a personal scans folder through it), one designated inbox folder scanned recursively, polling not push, review-before-import (never auto-imported, never a live two-way sync). See `ARCHITECTURE.md` §4a and `open-questions.md` #19.
- **Google Classroom adapter:** separate OAuth connection from Drive's (college Workspace account), no polling — launch scan + manual "Sync now" only. Course mapping is explicit/reviewed (name-match suggestion, never auto-assigned); once mapped, coursework/announcements/attachments auto-import with no per-item review. Attachments reuse the `resources` table; assignments mirror into `deadlines`. See `ARCHITECTURE.md` §4b and `open-questions.md` #20.
- **Hard guardrails (user-mandated, 2026-07-23):**
  1. Atlas makes **zero AI/LLM API calls internally**, ever. Claude Code (external process, user's own subscription) is the only reasoning engine — this is why MCP was chosen over Atlas calling Claude's API itself.
  2. **Zero paid/metered API usage anywhere in the project**, for anything, without explicit user approval first. All Google API access must stay on the free tier with no billing account attached.
  - Full reasoning in `CLAUDE.md` "Hard guardrails" section and `ARCHITECTURE.md` §0 — treat both as settled requirements, not preferences.
- **Continuity practice:** the user compacts/switches chats deliberately and will give a heads-up first in case last-minute doc changes are needed. This `STATUS.md` file exists specifically so a fresh session can pick up correctly without acting against prior decisions — keep it honest and current.

## Genuinely open (needs a decision, don't assume)

See `open-questions.md` for full detail. Nothing blocking right now — all previously-blocking items are resolved:

- **#8 College Google Workspace access** — resolved 2026-07-23. Verified via OAuth Playground: both Classroom and Gmail read scopes authorize cleanly against the college account, no admin block. Phase 3 can be scoped against the college account.

Still open, lower urgency (not blocking Phase 1): sync frequency/manual-vs-automatic (#2), sync-conflict policy (#3), course/semester archiving rules (#4), whether to build one-way PDF zoom memory (#10, needs a user decision — see "What's next" below), whether to route Office-file preview through Google Drive for real layout fidelity (#12, explicitly deferred to Phase 3 by the user).

## Session 24 additions

- **Fixed a real bug reported right after shipping notes**: Escape was unconditionally closing the note editor regardless of what had focus — reported as "I selected the title text, pressed Escape [to cancel the selection], and the note closed." Escape no longer closes the note editor at all (the X button is now the only way, matching how Notion itself behaves); the title input handles its own Escape by reverting an in-progress rename instead. Click-on-backdrop-to-close was also removed for the note editor specifically (kept for the read-only resource preview, where an accidental close is much lower-stakes).
- **Notes editor library replaced: Toast UI Editor → Milkdown (`@milkdown/crepe`).** The user's feedback ("really really basic... too raw and unpolished") plus two concrete complaints (no `-`/`*`/`1.` list-shortcut typing, no toolbar active-state indication) turned out to be real, verified gaps in Toast UI Editor's default WYSIWYG mode — confirmed via hands-on browser testing (typed `- ` and got literal text, not a bullet; pressed Ctrl+B and the Bold button never highlighted), not assumed. Before ripping out the working Toast UI integration, ran a deep round of questions on the user's actual Notion usage (bullet-heavy notes not paragraphs, heading hierarchy, math equations, multi-level nested bullets, dividers, rarely linked between notes) to make sure whatever replaced it actually fit, then verified Milkdown/Crepe solves every one of these — including math via bundled KaTeX, which Toast UI had no equivalent for and the user explicitly needs for academic notes — through the same kind of hands-on testing before committing to the switch. Dark theme (`frame-dark`) applied to match Atlas's UI. Same autosave behavior (600ms debounce via Crepe's `markdownUpdated` listener). `scripts/verify-app.js` updated with a regression guard specifically for the bug that caused the switch: asserts typing `- ` produces a real `<li>`, not literal text.
- Also switched dark-mode theming for the note editor from a manually-applied `theme: 'dark'` option (Toast UI) to Crepe's official `frame-dark` theme — no manual CSS overrides needed.

## Session 44 additions

Built the Google Classroom adapter, the next Phase 3 item after Drive per `ROADMAP.md` — the user said "go ahead with whatever is next on the roadmap." Planned first (plan mode), confirming three decisions with the user up front: course mapping is suggest-then-confirm (never auto-assigned), attachments reuse the `resources` table, and sync is launch + manual "Sync now" (no polling interval, unlike Drive).

- **`src/main/googleAuth.ts`** gained parallel Classroom scope/token plumbing (`CLASSROOM_SCOPES`, `google_classroom_refresh_token`, `isGoogleClassroomConnected`/`disconnectGoogleClassroom`/`getClassroomClient`/`authorizeGoogleClassroom`) rather than a second file — the loopback OAuth flow was already scope/account-agnostic, so only the scope list and settings key needed to vary.
- **Schema**: `courses` gained `source`/`classroom_course_id`; `assignments` gained `classroom_coursework_id`/`updated_at`; `announcements` gained `classroom_announcement_id`; `resources` gained `classroom_attachment_id`; `deadlines` gained `classroom_coursework_id`; new `classroom_pending_courses` table (mirrors `drive_pending_files`, with a `suggested_course_id` name-match column). `database.ts`'s `migrate()` also adds partial `UNIQUE` indexes on the four `classroom_*_id` columns above (`CREATE UNIQUE INDEX IF NOT EXISTS ... WHERE ... IS NOT NULL`) — needed as a real conflict target for the `INSERT ... ON CONFLICT DO UPDATE` upserts in the new sync code, since SQLite's `ALTER TABLE ADD COLUMN` can't add a `UNIQUE` constraint to an existing table directly.
- **New `src/main/googleClassroom.ts`**: `scanClassroomCourses()` (lists Classroom courses, stages unmapped ones with a name-match suggestion, reconciles stale pending rows), `syncClassroomCourseworkForMappedCourses()` (pulls courseWork/announcements for every already-mapped course, upserts by external ID, mirrors assignments into `deadlines`, imports Drive-file attachments when Drive is also connected), `scanClassroom()` orchestrator, plus the pending-course list/ignore/link functions. A `registerAttachmentSaver()` injection point avoids a circular dependency with `main.ts`'s file-path helpers.
- **`main.ts`**: new `classroom:*` IPC block (`isConnected`/`connect`/`disconnect`/`syncNow`/`listPendingCourses`/`ignorePendingCourse`/`mapCourseToExisting`/`mapCourseToNew`), `scanClassroomAndNotify()` called once at launch (no `setInterval`), and `classroom:mapCourseToNew` reuses the exact folder-creation steps `courses:create` already has rather than duplicating them in `googleClassroom.ts`.
- **UI**: a Dashboard "Google Classroom" widget (connect/disconnect/"Sync now"/pending-count/"Review new courses") and `#classroom-review-overlay`, structurally copied from the Drive review panel including its CSS Grid alignment fix (bulk-header row and per-course rows share one `grid-template-columns`) and the `:not([hidden])` display-scoping pattern for every new hidden-toggled button, per `CLAUDE.md`'s three-strikes CSS rule.
- **Judgment call, made explicit rather than silently assumed**: once a Classroom course is mapped, new coursework/announcements auto-import with no per-item review (unlike Drive) — the ambiguity a review step resolves (which course? what type?) doesn't apply once Classroom's API is already returning typed, course-scoped data. Documented in `ARCHITECTURE.md` §4b and `open-questions.md` #20 as a decision, not asserted as obviously correct.
- **Known gap, logged not silently dropped**: Classroom announcements have no UI surface yet (no due date, so nothing to mirror into the already-visible `deadlines` timeline) — flagged as a fast-follow in `open-questions.md` #20.
- Docs updated in the same change: `ARCHITECTURE.md` (new §4b), `ROADMAP.md` (Classroom bullet ticked), `open-questions.md` (#2 half-resolved for Classroom, new #20 for the adapter's decisions), this file.
- Verification: `npm run build` after each chunk, one full `npm run verify` pass before committing (existing suite, no regressions — Classroom has no automated script coverage, same as Drive, since both need a live OAuth account; verify manually with the user against their real college Workspace account next).

**Follow-up, same session** — the user connected Classroom for real (college account, had to add themselves as a Google Cloud OAuth test user first, since the app stays in Testing publish status) and found two real problems through actual use:

- **Bug: confirming a course mapping left it empty.** Linking a Classroom course only set `courses.classroom_course_id` — coursework didn't actually pull until the next launch or "Sync now" click. Fixed by triggering an immediate `scanClassroomAndNotify()` right after both `classroom:mapCourseToExisting` and `classroom:mapCourseToNew` succeed, same reasoning `setDriveFolder` already uses. Also extended the `classroom:changed` renderer handler to refresh whatever page is currently showing (course detail/dashboard/courses list), not just the pending-count badge, so the delayed sync's result is actually visible without a manual page reload.
- **Workflow mismatch surfaced, not a bug**: the user tested by mapping some already-past Classroom courses, which created brand-new Atlas courses (since no matching Atlas course existed to link to) — not what they wanted. Their actual intended workflow is the reverse: pre-create *next* semester's Atlas courses from `ashoka-planner` first, then use Classroom's "map to existing" (already built) to link rather than create. The 5 test courses created during this (`ECO-2202`, `ECO-2400`, `ENT-1011`, `ENT-1004`, `FC-0201`) were deleted per the user's request — via a one-off script run through Electron's Node runtime (`ELECTRON_RUN_AS_NODE=1`) against the real `Atlas-Storage/atlas.db`, mirroring `courses:delete`'s exact logic (remove the on-disk folder, cascade-delete via FK), then removed the scratch scripts. **Caution flagged for future sessions**: a first attempt to run a read-only query via plain `electron.exe <script>.js` accidentally launched real app/GUI code and threw a JS error the user had to close; a follow-up cleanup used a blanket `taskkill /F /IM electron.exe`, which — had the user's real Atlas instance been open — would have force-closed it. The user confirmed no data was lost (autosave, and they always close Atlas before sending a prompt), but the right tool for one-off DB scripts is `ELECTRON_RUN_AS_NODE=1 electron.cmd <script>` (plain Node, no app/GUI boot at all) — used correctly for the deletion itself, and should be the only approach going forward, never a blanket process kill.
- **Built the missing half of the workflow: Ashoka Planner course import** (`open-questions.md` #15, researched in an earlier session, built now) — a genuinely one-shot, user-invoked action (explicit per the user: "a process i invoke... not an automated process"), never polled. New `src/main/ashokaPlanner.ts` reads the user's separate `ashoka-planner` app's own SQLite file (path picked once via native dialog, remembered in `app_settings`) for `slot` rows with `status = 'secured'`, joined against `course_description` (by section code) and `student_profile.current_semester` — schema verified directly against that app's own `scripts/pipeline/schema.sql`/`registration_server.py` (extracted its zip to check, not guessed from the earlier research summary). New `courses.description` column holds the imported overview text. UI: an "Import from Ashoka Planner" button on the Courses page toolbar opens a plain checkbox review list (already-imported courses, matched by code + term, shown disabled to prevent duplicates on re-run); "Import selected" creates real Atlas courses via the same folder-creation steps `courses:create` uses. See `ARCHITECTURE.md` §4c.
- Docs updated again in this follow-up: `open-questions.md` #15 resolved with a built summary, `ARCHITECTURE.md` new §4c plus a note in §4b about the immediate-sync fix and the workflow clarification, `ROADMAP.md` new bullet, this file.
- Verification: `npm run build` clean, full `npm run verify` pass, no regressions.

## Session 43 additions

- **Built the Google Drive adapter end-to-end** (`ARCHITECTURE.md` §4a, `open-questions.md` #19) — the user created their own Google Cloud project/OAuth client (personal account, Desktop app credential type) and walked through the console setup live with guidance; the downloaded `client_secret_*.json` was copied into `Atlas-Storage/config/google-oauth-client.json` (new `getConfigDir()`/`getGoogleCredentialsPath()` in `paths.ts`), never the repo.
- **New `src/main/googleAuth.ts`**: loopback-redirect OAuth flow (RFC 8252 — the old out-of-band flow Google retired), a local HTTP server on an ephemeral port catching the browser redirect, refresh token stored in `app_settings`. Verified for real against the user's own Google account — confirmed connected before building anything further on top. Noted for the user upfront: staying in Google's "Testing" publishing status (skipping formal verification, which would need a privacy policy and a security review for Drive's scope — real overkill here) means refresh tokens expire after ~7 days, so periodic one-click reconnection is expected, not a bug.
- **New `src/main/googleDrive.ts`**: folder link parsing/validation (`setDriveFolder`), recursive scan (`scanDriveFolder` — added after the user asked for subfolder support so they could organize the inbox folder, e.g. one subfolder per course, without losing detection), download (`downloadDriveFileContent`), and the pending-file lifecycle (`listPendingDriveFiles`, `ignoreDrivePendingFile`, `removePendingDriveFile`). New schema: `resources.drive_file_id`/`notes.drive_file_id` (dedup on future scans) and a `drive_pending_files` table (`drive_file_id` UNIQUE, `ignored` flag) — all via the established `ALTER TABLE`-if-missing migration pattern in `database.ts`.
- **Import reuses the exact same buffer-based paths manual upload/drag-and-drop already had** — `importBufferIntoCourse` (Resource), `importScanBufferIntoNote` (Note that's a scan/image), and a new `importTypedNoteFromBuffer` (Note that's plain text/markdown) — a downloaded Drive file is just a buffer, no new import logic needed, just new callers.
- **Review panel, not auto-import**: a Dashboard "Google Drive" widget (connect/disconnect, folder link input, pending count, "Review new files") opens a modal listing every pending file with a per-row course-select + Resource/Note type-select, a shared bulk course/type selection applied to every checked row, and (added on request) a per-file and bulk **Ignore** action — skips a file without downloading it, and it stays skipped (`ignored = 1`, not deleted, so the table's UNIQUE constraint keeps a future scan from re-adding it as new).
- **Deletion handling, clarified via direct Q&A with the user**: deleting an imported file's Drive original does nothing to the local copy (one-time import, not live sync); deleting an unreviewed pending file gets cleaned up on the next scan instead of leaving a dead entry (added specifically after being asked "what happens if..."); re-uploading "the same" file isn't deduplicated unless the user uses Drive's own "Replace" (same file ID) rather than a fresh upload (new ID) — all now documented rather than left as surprises.
- **Real bug fixed along the way**: `scanDriveFolder`'s early-return on an empty folder listing skipped the stale-pending cleanup pass entirely — fixed so cleanup always runs regardless of whether new files were found.
- **Real TypeScript issue fixed**: `googleapis`' own dependency tree hoists a second, separately-typed copy of `google-auth-library`, so importing `OAuth2Client` directly caused a type-incompatibility error. Worked around by deriving the type from `google.auth.OAuth2` itself instead of importing it.
- **UI iterated multiple rounds on direct feedback** — the user called out sloppy spacing/lazy layout more than once (folder-link input+button, the bulk-actions row, a button losing its padding after an edit); fixed each time with real CSS (labeled input, a proper CSS Grid for the review panel so the bulk row and every file row share identical columns instead of drifting with filename length, consistent button sizing/padding using `box-sizing: border-box`).
- **Process correction, explicitly requested by the user**: stop running the full `npm run verify` suite after every small tweak (slow, not needed for CSS/UI-only changes) — use the fast `npm run build` type-check as the default instead, reserving full `verify` for before a commit or when something riskier changed. Also: commit and push incrementally as features land, not let a whole feature's worth of changes sit uncommitted — this session's work sat uncommitted through several rounds of iteration before the user flagged it.
- **Not covered by `scripts/verify-app.js`** — this whole feature needs a live Google account/OAuth token, which the automated suite can't supply. Verified manually instead, end-to-end, against the user's real account and Drive folder.

## Session 42 additions

- **Started a local-watched-folder "import as Note" prototype (schema-only), then reverted it.** Immediately after session 41, the user asked to think through the tablet/phone workflow properly (`open-questions.md` #19). Began extending Phase 1's Watched Folders with a per-folder "what should new files become" mode (Resource/handwritten Note/typed Note), assuming the folder would be a local Google-Drive-desktop-synced directory. The user then gave a fuller spec and was explicit on point 6: the folder must be an actual **Google Drive folder Atlas scans directly**, not a local synced directory — ruling out the local-`chokidar` approach entirely. Reverted the two touched files (`schema.sql`, `database.ts`) via `git checkout` before anything else was built or committed — clean, no stray state.
- **Reframed as the start of Phase 3, reordered.** Since scanning an actual Drive folder needs the real Drive API (OAuth, `googleapis`), this is squarely Phase 3's already-scoped "Google Drive adapter," not a Phase 1 extension — matching `CLAUDE.md`'s "don't build ahead of the current phase" guidance in the other direction too (don't build a phase-3-shaped feature disguised as a phase-1 tweak). Confirmed with the user via two questions: build Drive **first** within Phase 3 (ahead of Classroom/Gmail, since it's what solves the actual daily pain point) — yes; ~15–30s polling while the app is open as the realistic "near real-time" option, since true push (`changes.watch`) needs a public HTTPS endpoint that doesn't fit a local desktop app — yes. `ROADMAP.md` Phase 3 reordered and unchecked (was previously an unstarted placeholder list; now Drive is listed first with the concrete spec).
- **User's full spec for the Drive adapter** (`open-questions.md` #19): one designated Drive folder (e.g. "Atlas File Share"), scanned on every launch plus polled while running; new files surface in a review panel where the user assigns course + Resource/Note **per file** (individually or in bulk), with an explicit "later" dismiss and a persistent way to reopen the pending list rather than being forced to resolve it immediately; imported files are downloaded into local managed storage exactly like any other resource/note (Drive is the inbox, not a live reference).
- **Not yet started**: actual implementation. Needs the user to create their own free-tier Google Cloud OAuth client first (`open-questions.md` #7, already resolved as bring-your-own) — that's a manual step only they can do in Google Cloud Console, so it's the next concrete blocker before any Drive-adapter code can be tested.

## Session 41 additions

- **On-demand OCR for regular (non-handwritten) Resources PDFs**, closing out the item logged at the end of session 40 (`open-questions.md` #18) — the user asked directly right after that session's compaction. New `resources.ocr_text` column (migration in `database.ts`, same catch-up-`ALTER TABLE` pattern as every other schema addition) and two IPC handlers: `resources:runOcr` (reuses `extractTextFromScan` — already content-agnostic, no changes needed there) and `resources:saveOcrText` (writes the text and calls `rebuildSearchIndex()`). A "Run OCR" button on the Resources preview modal (`#preview-overlay`, visible only for `kind = 'pdf'`) shows a review panel (`#preview-ocr-review`) with **Discard**/**Save extracted text** — same never-silently-trusted shape as the Notes OCR flow, just Save-to-DB instead of Insert-into-note, since a resource has no editable content to insert into.
- **Opening a handwritten note now defaults to the original scan, not the editor.** The user pointed out that since a handwritten note's editor stays blank until OCR is run and accepted, opening straight into it looks broken — the scan should be what shows immediately, with the editor/OCR view one toggle click away. Refactored the scan-panel-loading logic out of `toggleNoteScanPanel` into a reusable `showNoteScanPanel()`, called both by the toggle and by `openNoteEditor()` for `is_handwritten` notes. The toggle button's icon/label now flips to reflect which view it would switch *to* ("View original scan" ↔ "View my notes"), same convention as the fullscreen button elsewhere.
- **Found and fixed a real focus-trap risk while updating the two-stage-Escape test for the new default**: the existing "single Escape closes when just viewing" test used to click a neutral spot inside the note overlay before pressing Escape — but with the scan (a PDF `<iframe>`) now the default view, that click landed on the iframe and stole keyboard focus into its separate document, so the top-level `keydown` listener never saw the Escape at all. Not just a test artifact — a real user clicking into the PDF viewer before pressing Escape would hit the same dead end. Fixed the test to avoid clicking into the iframe (a freshly-opened note has nothing focused already, so no extra click is needed); the underlying focus-trap itself is unfixed and pre-exists for the Resources preview's own PDF iframe too — noting it here rather than fixing speculatively, since it wasn't part of what was asked this session.
- `scripts/verify-app.js`: rewrote the "View original scan" assertions for the new default (scan shown + editor hidden immediately on open, toggle switches to editor and back), added a new Resources OCR block (run/discard leaves the resource unsearchable, run/save makes it searchable via `atlas.search`), and disambiguated an unrelated `@`-mention test whose query (`@sample`) started matching the new test PDF resource too. Full suite passes.

## Session 40 additions

- **OCR made opt-in per note, not automatic on import.** The user tested Phase 2's OCR against their actual handwriting and found local Tesseract's accuracy too poor to trust silently (expected — already documented as an accepted tradeoff of local/offline OCR, `ARCHITECTURE.md` §3, not a bug) — and separately pointed out Claude Code can already read a handwritten scan directly via vision today, no Atlas feature needed, which fits "Atlas owns data, Claude owns reasoning" better than automatic OCR ever could. Discussed as a real design question (`open-questions.md` #18) rather than silently changing the pipeline; the user chose to keep OCR (still useful for text-layer-less typed PDFs, a different case they raised) but make it opt-in and reviewed: "Import scan" now just stores the original file on a blank note (titled from the filename), and a new "Run OCR" button in the note editor runs extraction on demand, showing the result in a review panel with **Discard**/**Insert into note** — nothing is written until explicitly accepted, and accepting re-derives the title exactly like typing would. `notes.ocr_text` stays in the schema but is no longer populated by this flow.
- **Removed the Notes editor's "Expand width" button** (`#note-widen`) entirely — with the note editor now a global overlay (session 37) at a fixed, already-comfortable size, the separate width-expand level from the old docked-pane design no longer does anything useful; only "Fullscreen" remains. Removed the button, its icons (`NOTE_WIDEN_ICON`/`NOTE_WIDEN_COLLAPSE_ICON`), `toggleNoteWidth()`/`resetNoteWidenButton()`, the `.wide` CSS class, and the corresponding `verify-app.js` test.
- **Real bug caught and fixed: a 4th instance of the recurring `[hidden]`-vs-own-selector CSS trap** (`CLAUDE.md`'s standing check — previously hit on `#preview-overlay`, `#confirm-overlay`, `#zoom-controls`). `.split-preview-actions button` set `display: flex` unconditionally, which was harmless while every button in that container was always visible — until `#note-view-scan` (session 39) became the first one conditionally shown only for handwritten notes. The rule outranked the browser's default `[hidden] { display: none }`, so "View original scan" was visibly showing on every note, typed or not. Caught by a new verify-app.js assertion added specifically to check this (checking a typed note's `#note-view-scan` is actually hidden, not just assumed), not spotted by eye — fixed by scoping the rule to `:not([hidden])`, the same fix pattern as the prior three instances.
- **Answered: easier phone/tablet upload** (`open-questions.md` #19) — recommended the already-built Watched Folders feature pointed at a cloud-sync app's local folder, no new Atlas development needed for phone-scanned PDFs; flagged the caveat that watched folders currently import as Resources, not handwritten Notes, as a possible (not-yet-requested) follow-up.
- `scripts/verify-app.js` updated for the opt-in OCR flow: import now asserts a blank note + filename-derived title (not auto-populated OCR text); new assertions cover Run OCR's review panel, Discard leaving content untouched, and re-running + Insert both writing the text and re-deriving the title. Full suite passes.
- **"View original scan" is now a real toggle, not a split layout.** The user pointed out it shouldn't stack above the editor — showing the scan should replace the editor entirely, and toggling back should restore it, like flipping a page rather than shrinking both into half the space. Also made it mutually exclusive with the "Run OCR" review panel (running OCR while viewing the scan closes the scan view first).
- **Two-stage Escape in the note editor**, superseding the session-24 "Escape never closes a note" decision (still avoids losing an in-progress selection mid-typing, but no longer means Escape does nothing at all): first Escape while typing (title input or Milkdown surface) blurs out of editing; a second Escape then closes the note; a single Escape closes it outright if it was only being viewed (nothing ever focused). The title input's own Escape handler now calls `stopPropagation()` so it doesn't also trigger the close-check on the same keypress — see `ARCHITECTURE.md` for why that mattered.
- **Answered: does OCR work on regular typed PDFs (e.g. a scanned book with no text layer)?** Yes, technically — `extractTextFromScan`'s PDF path (rasterize each page, then OCR the image) doesn't care whether the source is handwritten or printed. It's just not wired up as a UI action for Resources yet, only for handwritten Notes via "Run OCR." Logged as a possible (not requested) future item in `open-questions.md` #18.

## Session 39 additions

- **Phase 2 built: handwritten notes via local OCR import.** The user said "ok go ahead with phase 2" after session 38's research/planning; this session implemented it per the plan (entered plan mode given the real scope — new dependencies, a new schema-adjacent data model decision, a new import UI flow).
- **Confirmed with the user before building**: primary input is multi-page PDF scans from Adobe Scan, individual photos are rare — this made PDF support a core requirement, not a nice-to-have, and settled "one imported file = one note" (matches how the user actually scans — one PDF per lecture) rather than needing a multi-page/multi-image-per-note data model. No schema migration needed: `notes.is_handwritten`/`image_path`/`ocr_text` already existed in `schema.sql`, added ahead of this feature but never populated until now.
- **New `src/main/ocr.ts`**: `tesseract.js` for OCR; `pdfjs-dist` + `@napi-rs/canvas` to rasterize PDF pages to images first (Tesseract only takes images). Verified the whole pipeline end-to-end with real spike scripts (a hand-written test PDF, a canvas-drawn test image) before wiring it into the app, not assumed to work from documentation alone.
- **Real compatibility problem hit and solved**: `pdfjs-dist` v6 ships ESM-only (no CommonJS build), but this project's main process compiles to CommonJS. A plain `await import(...)` gets rewritten by `tsc` to a `require(...)` call (confirmed by inspecting the actual compiled output), which throws `ERR_REQUIRE_ESM` on an ESM-only package — the same class of problem already hit and solved differently for `marked` (pinned to an older CJS-compatible version, `preview.ts`), but no such version exists for pdfjs-dist with this rendering API. Fixed by routing the dynamic import through `new Function('specifier', 'return import(specifier)')`, which hides the `import()` call from TypeScript's static transform so it reaches Node as a genuine dynamic import at runtime.
- **Vendored the OCR pipeline's own dependencies for genuine offline operation** — Tesseract.js's English language data (`eng.traineddata`, fetched from a CDN by default) and pdfjs's standard-14 font metrics (same default-CDN behavior) are both checked into `assets/` and copied into `dist/main/` by `scripts/copy-assets.js`, with `langPath`/`standardFontDataUrl` pointed at the local copies. Without this, "local, offline" OCR (the whole reason Tesseract was chosen over Cloud Vision, `ARCHITECTURE.md` §3) would have silently made network requests on first use.
- **Import UI**: "+ Import scan" on the Notes page, reusing the existing course-picker modal in a new `'scan'` mode (multi-select native dialog or multi-file drag-and-drop, each file becoming its own note, processed sequentially with progress text in the modal). **"View original scan"** toggle in the note editor overlay (handwritten notes only) reuses the exact same `Preview` type/rendering the Resources page's preview modal already has, via a new `notes:getScanPreview` IPC handler — a scan is just a file on disk, no different from a resource's.
- `scripts/verify-app.js`: two new real (not mocked) OCR round-trip tests — a checked-in 2-page PDF fixture (`scripts/fixtures/sample-scan.pdf`) via a new `ATLAS_TEST_SCAN_PATHS` test hook (mirrors the existing `ATLAS_TEST_UPLOAD_PATH` pattern, since native multi-select can't be driven by Playwright), and a synthetic canvas-drawn image via drag-and-drop — both assert real extracted text, the ✍️ badge, and the "View original scan" toggle. Full suite passes.
- `ROADMAP.md` Phase 2 fully checked off; also fixed unrelated pre-existing drift while touching the file (Phase 0's stale two unchecked scaffolding items, and the "(current)" phase marker still sitting on Phase 0 instead of Phase 3).

## Session 38 additions

- **No code changes — a planning/research session.** The user raised four forward-looking ideas; researched feasibility for the first two by actually reading the other repo's source (not guessing), then wrote all four up in `open-questions.md` #15–#17 rather than silently picking answers or building anything yet.
- **#15 — importing finalized courses from `ashoka-planner`** (the user's separate course-registration-planning app, private repo, same GitHub owner). Read `ashoka-planner`'s `scripts/db.py` and `scripts/registration_server.py` directly via `gh api`/raw content fetch to confirm the actual data shape rather than assuming one: its `data/planner.db` is a plain SQLite file; `registration_run`/`slot` rows with `status == 'secured'` are the user's actual finalized courses; a `course_description` table (keyed by section code) has real scraped course descriptions; `student_profile.current_semester` has the term label. Genuinely feasible as a one-shot "Import from Ashoka Planner" action — Atlas opening a second read-only `better-sqlite3` connection to that file, no network/OAuth involved. Would need one new `courses.description` column (small migration) and a user-confirmed review step before creating any courses, never a silent/automatic import.
- **#16 — auto-categorizing an uploaded file into its course.** Drew a hard line consistent with `CLAUDE.md`'s existing rules: a silent, always-correct auto-assign would be exactly the kind of AI-inference-written-back-as-fact the "Atlas owns data, Claude owns reasoning" rule prohibits (same reasoning already settled for folder watching, open question #11). A local, rule-based *suggestion* (course code/name matching against filename/extracted text, pre-selecting a guess in the existing course-picker modal but still requiring the user to confirm) is feasible now and doesn't need any AI call. A genuinely "smart" version — understanding an ambiguous file's actual subject matter — is Claude Code's job at runtime via the Phase 4 Context Builder/MCP, not something to build into Atlas itself.
- **#17 — Phase 2/3 reorder (handwritten notes vs. external sync).** Gave the user a recommendation (lean toward keeping the current order — OCR is self-contained/no external dependency, whereas sync is Atlas's first OAuth/external-service integration and arguably deserves the same "don't rush into it" treatment Phase 1 itself got) but left it as an open decision for them, not something to unilaterally reorder in `ROADMAP.md`.

## Session 37 additions

- **"Open in place, never redirect" — the core fix requested this session.** The user pointed out that clicking a file from Dashboard or a course detail page shouldn't navigate away to the Resources/Notes page first; it should just open right where they clicked. Two real bugs turned out to cause this:
  - `openPreview()` (Resources) called `showPage('resources')` unconditionally, even though `#preview-overlay` is already a global modal that renders fine over any page — the navigation was pure leftover, not load-bearing. Removed.
  - `openNoteEditor()` (Notes) genuinely *needed* `showPage('notes')`, because `#notes-editor-pane` lived docked inside `#page-notes`'s own DOM — an ancestor with `display:none` hides a `position:fixed` descendant too, so the "true fullscreen" trick couldn't have worked otherwise. Fixed at the architecture level: **moved the Notes editor out of the Notes page into its own global overlay**, `#note-overlay` (sibling to `#preview-overlay`, `#course-picker-overlay`, etc. near the end of `<body>`), the same pattern the Resources preview already used. `#notes-split` is now just the course rail + list (2 columns, matching `#resources-split`) — no third docked-editor column. `toggleNoteWidth()`/`toggleNoteTrueFullscreen()` now toggle `.wide`/`.fullscreen` classes on `#note-overlay` instead of `.pane-fullscreen`/`.true-fullscreen` on the old docked elements; `closeNoteEditor()` now refreshes whichever page is actually showing (Notes/Dashboard/course detail) instead of unconditionally re-rendering the Notes list.
  - `openDashboardDeadline()` had the same unnecessary-navigation bug — `#deadline-editor-overlay` is also a global overlay, so the `showPage('courses')` call before opening it was dead weight too. Removed, but `selectCourse(course)` is still called in the background (no page switch) since the viewer's "Edit" button reads `selectedCourse.id` for mention candidates — data needs to be right even though the page shouldn't change.
- **Dashboard right-click → "Go to."** Left-click on a Dashboard deadline/resource/"what changed today" item now always opens it in place (per the above); right-click adds a small custom-built context menu (`#dashboard-goto-menu`, not a native OS menu — this is pure in-renderer navigation, nothing for the main process to do) with a single "Go to" action that navigates to where the item actually lives — course detail's Deadlines section for a deadline, or the global Resources/Notes page pre-filtered to that course for a resource/note, reusing the same `resourcesCourseFilterId`/`notesCourseFilterId` + `showPage()` pattern the course detail page's own "View all" links already use — **and then also opens/previews the item there**, reproducing the original pre-session-37 click behavior exactly, just moved to right-click.
- **Phase 1 declared complete.** With this session's fixes, the user confirmed Phase 1 is done; `ROADMAP.md`'s one remaining unchecked item ("Course workspace UI: Overview, Resources, Assignments, Announcements, Notes, Deadlines, Files, Settings") is now ticked off, with a note that the course detail page already covers everything except Settings (nothing to configure) and Assignments/Announcements (deliberately deferred to Phase 3 per `open-questions.md` #13).
- `scripts/verify-app.js` updated: all `#notes-editor-pane`/`pane-fullscreen`/`true-fullscreen` references retargeted to `#note-overlay`/`.wide`/`.fullscreen`; new assertions that clicking a Dashboard/course-detail item opens in place *without* navigating away (checks `#page-dashboard`/`#courses-detail-view` visibility, not just that the overlay opened); new tests for right-click → "Go to" navigating correctly for both a resource and a deadline. Full suite passes.

## Session 36 additions

- **Third, smaller round of UI feedback, 5 items**, same day as sessions 34/35 — all verified with a throwaway Playwright screenshot script (deleted after use, per the established convention) plus a full `npm run verify` pass.
- **Resources list view: file kind moved to the far right**, no longer sitting immediately next to the course name. `.resource-kind` class added to the kind `<span>` in `resourceListItem()` (`renderer.ts`), `margin-left: auto` on `ul#all-resources-list.view-list li .resource-kind` (`styles.css`) pushes it to the row's right edge; name/course now have a small gap between them instead of the row's only spacing coming from the browser's implicit inline-element whitespace.
- **"Back to Courses" and "View all →" are now real buttons**, not text styled as a link. New feedback logged in memory (`feedback_clickable_looks_like_button.md`): the user does not want clickable text passing as a plain link — anything actionable should default to a bordered/background button. `.back-link` and `.link-button` both restyled to the app's existing secondary-button look (`border: 1px solid var(--color-border); background: var(--color-bg-elevated); padding`).
- **Notes editor fullscreen icons swapped to match their actual meaning.** The order (Widen → Fullscreen → Close) was already correct from session 35, but the icons were backwards: "Expand width" (`#note-widen`) was using the same corner-expand glyph as the Resources preview's fullscreen button, and "Fullscreen" (`#note-fullscreen`) was using an unrelated monitor icon. Fixed: `#note-widen` now uses a `↔`-style double-headed horizontal arrow (`NOTE_WIDEN_ICON`/`NOTE_WIDEN_COLLAPSE_ICON`), and `#note-fullscreen` uses the same corner-expand icon as `#preview-fullscreen` (`NOTE_FULLSCREEN_ICON`/`NOTE_EXIT_FULLSCREEN_ICON`, reusing the icon paths that used to belong to the widen button) — so the same visual language ("fullscreen") means the same icon everywhere in the app.
- **Right-click → "Open in browser" wired up on the course-detail page's inline Resources preview** (`#course-detail-resources-preview`, added session 35) — it only had a click-to-open handler before, unlike the main Resources list which already had a `contextmenu` listener calling `atlasApi.showResourceContextMenu()`. Same handler added, no new IPC needed (`resources:contextMenu` already existed in `main.ts`).
- `ARCHITECTURE.md` §12 extended with a short "Session 36: smaller polish items" subsection covering the above.

## Session 35 additions

- **Second round of detailed UI feedback, 9 items, same day as session 34.** Worked through each with visual verification (screenshots, and one synthetic-drop Playwright test) before moving to the next, same discipline as session 34.
- **Course-picker popup replaced with a real modal** (`#course-picker-overlay`) — the small anchored popup from session 34 cut off past the screen edge once there were more than a couple of courses (a real usability bug, not cosmetic). New modal has a search box that filters the course list live, shared between Upload and New Note via a `mode` param.
- **Drag-and-drop added to the Upload flow and directly to the Resources page.** Inside the course-picker modal, a dropzone (greyed out until a course is picked from the list above it) accepts a dropped file or a "Browse…" click (still the native file dialog). Dropping a file straight onto the Resources page list uploads immediately if the rail is filtered to one course, or opens the course-picker modal with the file already attached if "All Resources" is showing. New `resources:uploadBuffer` IPC path (`importBufferIntoCourse()` in `main.ts`) reads the dropped file's contents via `arrayBuffer()` in the renderer rather than relying on a real filesystem path — Electron's `File.path` exposure is deprecated/unavailable under `contextIsolation: true`, so this uses the same buffer-over-IPC pattern already used for note images.
- **Notes editor now has two distinct fullscreen levels.** The old single "fullscreen" button only widened the editor within the page (never covered the sidebar/topbar) — the user pointed out this wasn't real fullscreen. Split into "Expand width" (`#note-widen`, the original behavior) and "Fullscreen" (`#note-fullscreen`, new — pins the editor pane over the whole window via `position: fixed`, same mechanism the Resources preview modal already used). The "F" keybind now triggers genuine fullscreen.
- **Grid/List button order and toolbar position standardized** between Courses and Resources/Deadlines (was Grid-then-List on Courses, List-then-Grid on the other two). Resources gained a sort dropdown (Name/Recently added/Kind/Course) positioned identically to Courses' own sort-plus-toggle grouping. Courses' sort dropdown extended with Deadlines/Notes options.
- **Course archiving logged as a deferred item** in `open-questions.md` #4 (already an open question) — the user asked to defer building it but wanted it on record.
- **Electron's default application menu removed entirely** (`Menu.setApplicationMenu(null)`) — the odd-looking menu Alt revealed was Electron's auto-generated dev-oriented template (Reload/Toggle DevTools/generic Edit commands), none of it a real Atlas feature. Confirmed standard text-field editing (Ctrl+A/C/V, etc.) is unaffected via a direct Playwright check, not assumed.
- **"Upload file" → "+ Upload file"**, matching "+ New note"'s wording — folded into the course-picker modal rework rather than a separate one-line change.
- **Resources view-toggle row spacing fixed** — the toggle buttons had no margin below them before the resource list, crowding the first row/tile; now removed since the row was folded into the new sort/toggle toolbar which already has its own margin.
- **Course detail page: inline Resources/Notes previews**, not just a bare count button. Two small widget cards (reusing dashboard-widget styling) show up to 5 of the course's own most-recent resources/notes, each clickable to open directly, with "View all →" still available. Explicitly left to judgment by the user ("if you think the current functionality is enough then okay") rather than requested outright.
- `scripts/verify-app.js` extended throughout: course-picker modal search+select flow (replacing the old single-click popup assertions), two new drag-and-drop tests (synthetic `DragEvent`+`DataTransfer`, since Playwright can't drag a real OS file), the "Expand width" vs "Fullscreen" distinction for Notes, Resources sort-by-name, and the course detail preview widgets + "View all" navigation. Full suite passes.

## Session 34 additions

- **Large round of direct UI/UX feedback after the session 33 reskin**, 17 items in one message. Worked through them methodically rather than batching blind — read/rebuilt/screenshotted after each substantive change rather than making all 17 changes and checking once at the end, per the user's explicit "take your time, quality over quantity."
- **Removed the Settings page** (added in session 33 reskin) — user pointed out there's nothing to configure yet, so a page holding only the data-folder path wasn't worth a nav item. Removed the unused `app:dataDir` IPC handler and `getDataDir` API along with it (dead once nothing called it).
- **Removed "Search" from the sidebar** — it was redundant with the top-bar search box already on every page.
- **Real bug found and fixed: one course's Resources/Deadlines counts were invisible on its card, the other's weren't.** Root cause (confirmed via a throwaway Playwright script measuring `getBoundingClientRect()` against the user's real data, not guessed from the screenshot): a stale CSS rule, `ul#course-list li { display: flex; ... }`, left over from before the course-card grid was built in session 33. Being an ID selector, it silently outranked `.course-card`'s own layout rule and turned every card into a flex row, squeezing the counts row into a narrow strip pushed past the card's right edge and clipped by `overflow: hidden` — visible or not depending on exact content width, which is why it looked like only one card was affected. Deleted the stale rule (the flat-list layout it was written for doesn't exist anymore) and consolidated the `.selected` outline into one rule covering both grid and list view.
- **Minimal custom scrollbars app-wide** — thin, no track background, rounded thumb, via `::-webkit-scrollbar` rules (Chromium ignores the standard `scrollbar-width`/`scrollbar-color` properties but they're included for completeness).
- **Grid/List is now the one naming convention everywhere** a view toggle appears (Courses, Resources, Deadlines) — Resources/Deadlines previously called the same concept "List/Icons," inconsistent with Courses' "Grid/List." The `viewMode` preference (`renderer.ts`) is now typed `'list' | 'grid'` (was `'list' | 'icons'`; an old saved `'icons'` value is still honored on read so existing preferences aren't silently reset). All toggle buttons are now icon-only (monochrome SVG), not a mix of text labels.
- **Window size/position/maximized state persists across launches** (`loadWindowState()`/`saveWindowState()` in `main.ts`, stored as JSON in `app_settings`) — first run with no saved state defaults to maximized, per the user's request that the app not always reopen at a fixed 1280×800.
- **Resources preview reverted from a docked pane back to a full overlay modal** — the docked pane (built in session 33) left too little width for the resource list next to it for what the content actually needed; the user asked for the pre-reskin overlay behavior back. Fixed the F-key/fullscreen-button bug as part of this: the docked pane's "fullscreen" only collapsed the sibling columns without the pane itself growing, so pressing F visibly did nothing — full-window fullscreen on the modal (`.fullscreen` on `#preview-overlay`) actually works now.
- **Notes editor fullscreen bug fixed separately**: `#notes-split`'s own `grid-template-columns` rule (an ID selector) was outranking the `.pane-fullscreen` class rule meant to collapse it to one column, so the editor pane visibly shifted right into a still-reserved empty column instead of filling the width. Fixed with a more specific `#notes-split.pane-fullscreen` rule.
- **Upload/New Note now open a small course-picker popup** (`pickCourse()`) anchored under the button, instead of a permanent `<select>` sitting in the page header — the user found the header select confusing, since it looked like a second course filter alongside the Resources page's own course rail. Skips the popup entirely when there's only one course (nothing to actually choose). Notes' "+ New note" got the same treatment for consistency.
- **Notes page gained its own course-filter rail** (same `renderCourseRail()` component Resources uses), addressing "no way to filter notes by subject."
- **Added a sidebar collapse toggle** (icon-only rail, chevron flips direction), persisted via `app_settings` like every other view preference.
- **Dashboard reworked to address "looks so empty"**: added a stat strip (total courses/resources/notes, upcoming-deadline count — `dashboard:stats` IPC handler) above the four list widgets, which gives the top of the page real visual weight regardless of how short any one list is; and pulled "My courses" out of the four-widget grid into its own full-width row of colored chip-cards (avatar + name + code + count) instead of a plain vertical list, since a course has real visual identity a resource/deadline/activity row doesn't.
- **Courses page restructured: grid-only, with a genuinely separate course detail view.** Clicking a course card no longer expands an inline drill-down below the still-visible grid — it navigates to `#courses-detail-view` (with its own "← Back to Courses" control), showing that course's Deadlines/Watched folders plus two link buttons into Resources/Notes pre-filtered to the course. Matches the user's explicit ask and a mockup they shared of a dedicated course page — built using only real, already-existing Atlas features (no fabricated Assignments/Schedule/Announcements tabs from the mockup, consistent with the standing `open-questions.md` #13 decision).
- **CSS polish pass**: removed several genuinely dead rule blocks left over from before earlier reworks (`.courses-header`, `#dashboard-section`, `#settings-section`, `#note-list.*`, `ul#course-list li`/`li.selected`) — found systematically (a script diffing every CSS id/class selector against actual usage in `index.html`/`renderer.ts`), not just eyeballed. Re-screenshotted every page in both themes to confirm no visual regressions.
- `scripts/verify-app.js` updated throughout for all of the above — course-detail-view navigation and "Back to Courses," the grid/list rename, the preview-overlay/course-picker changes, sidebar collapse + persistence, window-state persistence (a genuine 3-launch test: resize, close, relaunch, confirm), and the new dashboard stat strip. Full suite passes.

## Session 33 additions

- **UI reskin**, per the user's request after sharing a ChatGPT-generated mockup (sidebar nav, card dashboard, light/dark toggle, colored course icons — "looks like an actual app dashboard"). Before building, flagged that several things in the mockup (Calendar, Tags, Trash, Notifications, a separate Assignments concept) are genuinely new features, not restyles, and asked how much to build now — the user chose reskin-only, so none of those were added; the Assignments question specifically would have reopened a decision already made deliberately in `open-questions.md` #13.
- **First pass shipped, then the user rejected it as too shallow.** Verbatim: sidebar tabs "don't do anything, they look like just decorative elements," the note editor "should follow the same theme as the app," and the Courses/Resources/Notes pages didn't closely replicate the mockup screenshots they'd sent for those specifically ("i see a very lazy effort which results in a barely done shabby work... please try i dont see the effort"). They explicitly authorized adding small functionality if that's what real fidelity required. This triggered a full second pass, described below, rather than incremental polish on the first one.
- **Real page routing** (`showPage()`/`AppPage` in `renderer.ts`): five actual pages — Dashboard, Courses, Resources, Notes, Settings — with exactly one visible at a time (`.app-page` show/hide + `.sidebar-nav-item.active`), replacing the first pass's `scrollIntoView` shortcut. Sidebar "Search" is not a page — it just focuses the search box over whatever page is showing.
- **Courses page rebuilt as a card grid** (`li.course-card`: colored avatar, name, code, "⋯" context-menu button, resource/deadline counts), with grid/list view toggle and a name/resource-count sort. "+ Add course" is now a small toggle button revealing a normally-hidden form, rather than a form that's always on screen — direct response to "i dont need an add course button always because i will only add like 5 courses a sem."
- **Resources and Notes promoted to genuine global (cross-course) pages**, matching the dedicated mockups the user sent for each: Resources gets a course rail + kind-filter chips (All/PDF/Document/Image/Presentation/Spreadsheet/Other) + list, Notes gets a Today/This week/Older grouped list. Both replace their old modal preview/editor with a **docked inline pane** (`.split-preview`) sitting beside the list in a `.split-layout` grid — "fullscreen" now means collapsing the sibling rail/list columns rather than covering the whole window. New `resources:listAll`/`notes:listAll` IPC handlers back the global lists.
- **The Milkdown note editor genuinely follows the app theme now** — the earlier "pin it dark" workaround was replaced with a real fix: Crepe's `frame`/`frame-dark` theme files turned out to be nothing but `--crepe-*` CSS variables scoped to `.milkdown` (confirmed by reading the actual files in `node_modules`), so both variable sets are now inlined into `styles.css` under the same `:root[data-theme='light']` gate the rest of the app uses, and the old static `import '@milkdown/crepe/theme/frame-dark.css'` was removed. Verified via screenshot — light theme now shows a correctly light-themed editor.
- **Two more real instances of the recurring `[hidden]`-vs-own-selector CSS bug** (documented in `CLAUDE.md`) turned up while building the docked panes: `.split-preview` and `form#course-form` each set `display` unconditionally on their own selector, keeping them visible even while `hidden`. Fixed the standard way (`:not([hidden])`); both caught via screenshot, not assumed.
- **Notes list going stale after autosave-derived title changes** — invisible in the old modal (list wasn't on screen while editing) but a real, visible bug once Notes became a persistent split view; fixed by re-rendering the notes list from `scheduleNoteSave()`'s autosave callback.
- **Duplicate element IDs across the now-separate Deadlines/Resources view toggles** (`#view-list`/`#view-icons`) caught by a real Playwright timeout, not by inspection — fixed with dedicated `#deadline-view-list-toggle`/`#deadline-view-icons-toggle` buttons in the Deadlines section, both wired to the same shared `setViewMode()`.
- Dashboard's "My courses" card and its two-stacked-row list layout (from the first pass) carried forward unchanged into this rework.
- `scripts/verify-app.js` rewritten substantially for the new page architecture: real page-switching assertions, course-form toggle, course grid/list view + sort, course-detail drill-down, Resources kind-filter chips and course-rail filter, dashboard navigation landing on the correct page for courses/resources/deadlines. Full suite passes end-to-end.
- See `ARCHITECTURE.md` §12 for the full architectural writeup.

## Session 32 additions

- **Dashboard v1 built** — the last remaining checked-off Phase 1 item on `ROADMAP.md` (Course workspace UI as distinct tabs is the one item left, deliberately deferred). Global overview above the course list: **upcoming deadlines** (incomplete, has a due date, across every course, soonest first, capped at 8), **recently added resources** (across every course, capped at 8), and **what changed today** (resources added + notes updated, filtered to the user's local calendar day — deliberately compared in the renderer, not via SQLite's `date('now')`, since that's UTC and can disagree with local "today" depending on timezone/time of day).
- New IPC handlers (`dashboard:upcomingDeadlines`, `dashboard:recentResources`, `dashboard:recentActivity`) joined against `courses` for display names.
- Refreshes opportunistically: `renderResources()`/`renderNotes()`/`renderDeadlines()` each now also fire-and-forget `renderDashboard()`, rather than adding a dedicated hook at every one of the many resource/note/deadline mutation call sites — those three functions already run after every mutation that could change what the dashboard shows.
- Clicking any dashboard item selects its course and opens the resource/note/deadline directly in the existing preview/editor/viewer UI — same navigation pattern as global search results and deadline description mentions.
- Also ticked off "Unified Resource Library" on `ROADMAP.md`, noticing it was already effectively done (manual upload and folder watching already share one per-course resource list/table regardless of source) but hadn't been marked as such.
- `scripts/verify-app.js` extended: confirms all three widgets show the expected data for the test course, and that clicking a dashboard deadline/resource opens the deadline viewer/resource preview respectively.
- **Also logged, not fixed**: the user flagged (with a screenshot) multiple scrollbars appearing at once in some views — looked like Chromium's PDF-viewer scrollbar nested alongside the page's own. Explicitly asked to defer this, not fix it now — logged as `open-questions.md` #14 for a later dedicated UI/layout pass.
- **Follow-up round, same session — 4 small requests:**
  1. **Visual separator (`•`) between course name and due/changed date-time** in the deadlines and activity dashboard widgets, so e.g. "Data Structures & Algorithms Tomorrow at 23:45" doesn't read as one run-on clause.
  2. Answered: **"Recently added resources" is the last 8 by `added_at`, not a time window** — could be from just now or from weeks ago, whichever 8 were added most recently.
  3. Answered: **"What changed today" only covers resources added and notes updated today** — not resource deletions, and not deadlines (which don't currently have a created/updated timestamp column at all).
  4. **"F" toggles fullscreen for an open resource preview**, same as clicking the fullscreen button — guarded to not fire while a text field has focus, so typing a literal "f" (e.g. into a deadline title) is never hijacked. Extended to the note editor too, on request — the Milkdown editing surface itself counts as a text field for this guard, so typing "f" while actually writing a note still just types "f"; blurring out of it (clicking anywhere non-editable) re-enables the shortcut.
  - `scripts/verify-app.js` extended: confirms pressing "f" enters and then exits preview fullscreen, and separately, that typing "f" inside the note editor produces a literal "f" (not a fullscreen toggle) while pressing "f" after blurring out of the editor does toggle it.

## Session 31 additions

- **Deadlines UI built**, per the user's explicit choice of "deadlines/assignments UI next" after search. Before building, flagged a real scoping overlap: the schema has both `deadlines` (PRD §12's unified timeline) and a separate `assignments` table (meant for synced Classroom data with a submission-status field), and with no sync built yet a manual "Homework 3" entry could go in either. Asked the user directly rather than picking silently — they chose Deadlines only, deferring a separate Assignments tab to Phase 3 (when `assignments`' status field actually has real synced data to hold). See `open-questions.md` #13.
- New per-course "Deadlines" section: add with title/kind/optional due date, toggle complete via checkbox, delete via the same native right-click pattern as resources/notes/watched folders. Sorted incomplete-first, then soonest-due-first, no-due-date items sorted last within each group — surfaces what's actually actionable at the top.
- Due dates are date-only (`<input type="date">`), stored as plain `YYYY-MM-DD` strings and parsed back out with explicit year/month/day components rather than `new Date(str)`, which would otherwise render a day early in negative-UTC-offset timezones.
- Not wired into global search — deadlines are a todo-style list, not longform content search is meant to help re-find.
- `scripts/verify-app.js` extended: confirms sort order (soonest-due-first, no-due-date last), that completing an item both applies the struck-through style and re-sorts it to the bottom, and that delete removes it.
- **Follow-up round, same session — 9 requests after seeing the first Deadlines pass:**
  1. **Menu bar hidden by default** (`autoHideMenuBar: true`), Alt reveals it — saves screen space.
  2. **Due date can be typed (`dd-mm-yyyy`) as well as picked** from a calendar — both, not one or the other. A visible text field plus a 📅 button that opens a hidden native `<input type="date">`'s picker via `showPicker()`, synced both ways.
  3. **Optional time** — `due_at` now holds `'YYYY-MM-DD'` or `'YYYY-MM-DDTHH:MM'` depending on whether a time was set, rather than a schema column that's usually empty.
  4. **"Today"/"Tomorrow" replace the date** for near-term deadlines; anything further out still shows a plain date.
  5. **Time is appended to the formatted due label** when set (e.g. "Today at 2:30 PM").
  6. **Icon view added for deadlines**, sharing the same app-wide list/icon toggle resources/notes already use (no new separate toggle) — each `kind` gets its own icon.
  7. **Editing an existing deadline** — the add modal doubles as an editor: clicking a deadline opens a read-only view with an Edit button, which switches the same overlay into the form.
  8. **Optional free-text description field** added (`deadlines.description`, new column).
  9. **`@`-mention references in the description** — typing `@` autocompletes against that course's own resources/notes (modeled directly on Claude Code's own `@file` reference convention, which the user pointed to), inserting a `@[Title](resource:<id>)`/`@[Title](note:<id>)` token; rendered read-only as a clickable link that opens the referenced resource/note.
  - Caught and fixed a real instance of the documented recurring CSS bug (`CLAUDE.md`) while building this: `#deadline-date-error` set `display: block` unconditionally on its own ID selector, which outranked the `[hidden]` default and kept the "Invalid date" message visible even when the field was empty/valid. Fixed by scoping it to `#deadline-date-error:not([hidden])`, per the standing check.
  - `scripts/verify-app.js` extended substantially: the whole deadlines test block now drives the add/edit modal instead of the old inline form, plus new coverage for typed-date + time formatting, the icon view, the mention autocomplete dropdown appearing and inserting a token, clicking a rendered mention to open the referenced resource, editing an existing deadline in place (not duplicating it), the "Today" label, and that the menu bar is set to auto-hide.
- **Second follow-up round, same session — 2 more requests:**
  1. **Keyboard navigation for the mention autocomplete** — Arrow Up/Down highlight a suggestion (mouse hover stays in sync), Enter inserts the active one (or the top match if none arrowed to), Escape dismisses just the suggestion list without closing the deadline editor. Same pattern as the search results keyboard nav (session 30).
  2. **Ctrl+L jumps focus to search from anywhere**, matching a browser's address-bar shortcut — the user's own reference point. Deliberately not hinted anywhere in the UI, per their explicit "don't clutter the interface with this" request.

## Session 30 additions

- **Global search built** — the last item in Phase 1 besides Dashboard v1. Asked the user which of the two remaining Phase 1 items to build first, given Dashboard v1 has a real data-availability gap (it's supposed to show upcoming deadlines, but there's no UI yet to create deadlines/assignments); they chose search first, since it has real content to search right now (notes, uploaded resources) with no such gap.
- New `search:query` IPC handler (`main.ts`) runs the query against the FTS5 `search_index` table, joined to `courses` for display, ranked by FTS5's own `rank`, capped at 30 results, with a `snippet()`-generated highlighted excerpt.
- `search_index` is kept populated via a full rebuild (`rebuildSearchIndex()`) called after every resource/note mutation across all their call sites (manual upload, folder watching, course-storage watching, reconciliation, notes CRUD, course delete) and once at app startup — chosen over hand-maintaining incremental inserts/updates/deletes at each of those ~15 call sites, which would be easy to get out of sync at just one of them. Full rebuild costs single-digit milliseconds at this app's actual scale. See `ARCHITECTURE.md` §9 for the full reasoning.
- Resource bodies are indexed for `text`/`markdown` kinds (read straight off disk); other kinds (PDF/DOCX/etc.) are searchable by title only, since Atlas has no text-extraction pipeline for them yet.
- New search box in the app header (`#search-input`), debounced 250ms, dropdown results with course name + highlighted snippet, explicit "No matches" state, closes on Escape or an outside click. Clicking a result selects its course and opens the matching note/resource directly in the existing editor/preview UI — no separate search-result view built.
- Snippet highlighting is done safely: the main process wraps matches in literal `<mark>` text, the renderer escapes the whole snippet as HTML first (a note/resource body is user-authored text and could itself contain `<`/`>`/`&`) and only then turns the two known-safe marker strings back into real tags, before it's dropped into the results dropdown via `innerHTML`.
- `scripts/verify-app.js` extended: searches for content in the still-present markdown resource and confirms the right result appears with the right course name and highlighted snippet, that clicking it opens the resource preview and clears the search box, and that a query with no matches shows "No matches" rather than an empty dropdown.
- **Keyboard navigation for search results** (follow-up, same session): user asked to minimize mouse use where possible. Arrow Down/Up now move an "active" highlight through the results list (mouse hover keeps it in sync too), and Enter opens whichever result is active — or the top result if none has been arrowed to yet, so confirming an obvious top match doesn't require an extra Arrow Down first. Escape still clears/closes as before.

## Session 29 additions

- **Note images moved from a flat shared folder to per-course storage.** The user said explicitly they didn't like images living in one global `Atlas-Storage/note-images/` folder and wanted them under `Atlas-Storage/<course>/notes/note-images/` instead, alongside that course's exported `.md` files. `getNoteImagesDir()` (`paths.ts`) now takes the course's folder name; `notes:saveImage` looks up the note's course to know where to write; the served URL now carries the course id (`/note-image/:courseId/:filename`) so the local HTTP server (which has no other way to know which course an image belongs to) can resolve the right folder per request.
- As a side benefit, the exported `.md`'s image link got simpler: since the image now lives right next to the exported note (both under the same course's `notes/` folder), the relative link is just `note-images/<uuid>.png` instead of reaching back up three levels (`../../../note-images/...`) to a shared top-level folder.
- **Migrated the one existing real image** in the user's actual `Atlas-Storage` data (`Data Structures & Algorithms/notes/note-images/`) by hand: moved the file into the new per-course location, rewrote the note's stored `content_markdown` URL in the DB, and fixed the already-exported `.md`'s relative link — confirmed both the DB and the on-disk export now agree before considering it done.
- `scripts/verify-app.js` updated: the relative-image-link assertion now expects `note-images/...` (not `../../../note-images/...`); full suite re-run and passing, including the image-survives-a-restart and browser-view checks.

## Session 28 additions

- **Removed image duplication in the note export feature.** The user noticed the same image existed in both `note-images/` and a note's exported `<title>.assets/` folder and asked whether one file could serve both jobs. It can: the exported `.md` now links to the shared `note-images/` file via a relative path (`../../../note-images/<uuid>.png`) instead of copying it — verified the relative link actually resolves to the same file before calling it done. Explicit trade-off, stated to the user: this only resolves as long as the exported file stays under `Atlas-Storage` (or `note-images/` travels with it); a single note file copied completely elsewhere on its own would have a broken image link. No `.assets/` folders are created anymore.
- **Real bug caught by the test suite while making this change**: a note whose first line was *only* an image (no other text yet) derived its title — and therefore its exported filename — from the image's raw markdown syntax (alt text + URL), producing a garbage filename like `![1.00](http---...).md`. Fixed by having `deriveTitleFromMarkdown()` skip an image-only line and fall through to the next one (or `Untitled` if the whole note is just images).
- Manually cleaned up the one real instance of this in the user's actual data (`Data Structures & Algorithms/notes/This is a test.assets/`) — re-triggered the note's export to pick up the new relative-link format, confirmed the `.md` content updated correctly, then removed the now-orphaned `.assets` copy.
- `scripts/verify-app.js` extended: confirms no `.assets` folder is created after inserting an image, that the exported file's image link is a relative path resolving to a real file in `note-images/`, and regression-guards the image-only-first-line title bug specifically.

## Session 27 additions

- **Notes now export to a plain `.md` file**, per the user pointing out a real gap: notes lived only in the database, so there was no way to retrieve/use one externally. Discussed the design trade-offs explicitly before building (not assumed): confirmed one-way mirror only (Atlas never reads the file back — the DB stays the real editing source, matching how the user actually wants to work), and confirmed images should use relative paths so the exported file is genuinely viewable standalone, not just while Atlas is running.
- Exported to `files/<course>/notes/<title>.md`; embedded images copied into a matching `<title>.assets/` folder alongside it, with the note's local-server image URLs rewritten to relative paths in the exported copy only (in-app editing and the browser view still use the live server URL). Renaming a note renames its export and cleans up the old file/assets folder (`notes.exported_path` tracks the last-written path for this). Deleting a note removes its export too.
- **Real regression risk caught before it shipped**: every course's `files/<course>/` folder is already watched to auto-import manually-added files as resources (session 22) — without an explicit exclusion, Atlas's own exported note files would get picked up by that same watcher and re-imported as bogus resources. Added an exclusion for the `notes/` subfolder to both the `add` and `unlink` handlers, and verified directly (listed resources for a course with an exported note — confirmed empty) rather than assuming the exclusion was sufficient.
- `scripts/verify-app.js` extended: confirms the exported file exists with the right content after a save, that it's removed after the note is deleted, and that it never shows up as a resource.

## Session 26 additions

- **Real bug found and fixed: note images broke on every app restart.** The user reported an inserted image not loading after closing and reopening the app, or via the browser view. Root cause: Crepe's image block defaulted to a `blob:` URL (dead the moment the renderer process that created it ends), *and* even after fixing that to use a proper saved-file URL, the local HTTP server picked a random port every launch (`server.listen(0, ...)`) — since the image URL is baked directly into the persisted `content_markdown`, a previously-working image URL pointed at a port nothing was listening on anymore after any restart. Fixed both: images now save to `Atlas-Storage/note-images/<uuid>.<ext>` via `notes:saveImage` + Crepe's `ImageBlock.onUpload` config, served from the local server which now binds to a **fixed port (47823)** instead of an ephemeral one. Verified with an actual two-session restart test (not just reasoning about it) before considering it fixed.
- **Headings are bold by default, and Ctrl+B genuinely toggles it off** — implemented as a small custom ProseMirror plugin (not a CSS trick, which can't satisfy both halves of the ask at once — see `ARCHITECTURE.md` §8 for why). Verified through both ways of creating a heading (`# ` shortcut and picking H1/H2 from the slash menu), toggling off then back on, and confirming plain paragraphs are unaffected.
- **Slash-menu heading labels shortened to "H1"–"H6"** (were "Heading 1"–"Heading 6"), via Crepe's `BlockEdit` feature config — easier to scan/search per the user's request.
- **Title auto-derivation now strips inline formatting too** (bold/italic/strikethrough/code), not just block-level markers — a bolded first line no longer produces a title with literal `**` in it.
- **Notes now share the same list/icon view toggle as resources** — one app-wide setting governs both, per the user's "even notes should follow the list/icon thing" ask; no separate toggle added.
- **"Term"/"Semester" wording standardized to "Semester" everywhere**, and the course-creation dropdown no longer says "(optional)" — the user pointed out the inconsistency and that semester categorization is actually important, not incidental.
- `scripts/verify-app.js` extended: image upload → close → **full app relaunch** → confirm the image still loads (`naturalWidth > 0`, not just that the `<img>` tag exists) both in-app and via the read-only browser view; note list/icon toggle; bolded-first-line title stripping.

## Session 25 additions

- **Sans-serif fonts everywhere in the note editor** — Crepe's default theme used a serif font for headings/quotes, which looked inconsistent against Atlas's sans-serif UI. Overridden in `styles.css`, scoped to `.milkdown` elements.
- **"Open in browser" added for notes** — right-click a note → same context-menu pattern as resources. Deliberately **read-only**: an editable browser copy would mean two live editing surfaces (the desktop Crepe instance and the browser tab) writing to the same note with no conflict resolution, so this stays a reference/view-alongside-other-tabs feature, matching the role it already plays for resources. Renders via `marked`, with `$...$`/`$$...$$` math segments extracted and rendered through KaTeX before the markdown pass, then spliced back in (`marked` has no native math support). KaTeX's CSS/webfonts are served from two new local-server routes (`/katex.css`, `/katex-fonts/:file`) since the external browser tab can't reach Atlas's own bundled assets.
- **Title now auto-follows the first line of the note, Google-Docs style** — new `notes.title_is_manual` column (defaults to 0). Every autosave derives a title from the first non-empty line (markdown prefixes like `#`/`-`/`1.` stripped) and updates it, *unless* the user has ever typed into the title field directly, which flips `title_is_manual` to 1 and permanently decouples the title from content for that note. Verified the full sequence live: type first line → title follows it; change first line → title still follows; manually rename the title, then change the first line again → title correctly stays put. A blur on an *unchanged* title field doesn't count as an edit (guards against accidentally locking the title just by clicking into the field and clicking away).
- **Section reorder**: Notes now sits between Resources and Watched Folders (previously last), per the user's ask ("move notes above watched directories") — a quick fix ahead of the fuller layout rework the user said we could defer. Watched Folders is now its own top-level `<section>` (was a `<div>` nested inside Resources), with its own show/hide logic to match Resources/Notes.
- **Investigated, not built: `/h1` + Tab shorthand in the slash menu.** Crepe's slash menu filters by substring match against the full visible label ("Heading 1"), not fuzzy/alias matching — typing `h1` (no space) matches nothing and the menu just closes, confirmed via hands-on testing rather than assumed. Not worth a workaround: the equivalent live markdown shortcut (typing `# ` for H1, `## ` for H2, directly in the text, no menu at all) already works and is arguably faster than what the user described.

## Session 23 additions

- **Notes UI built** — the next Phase 1 item. Format/organization questions (`open-questions.md` #1) resolved through conversation with the user: Markdown (not rich text — the deciding factor was that notes get read by Claude Code via MCP, not exported to PDF like the user's old Notion workflow, and markdown is what Claude reads most naturally with no lossy rich-text-to-markdown conversion needed), flat per-course organization (no folders — matches how the user actually used Notion: course-level grouping plus a session-titled note like "W1L1", not a manually-built folder tree), live-rendering WYSIWYG editor (Toast UI Editor) rather than a raw-markdown/preview split.
- **Real architecture change: renderer now has a build step (`esbuild`).** The renderer previously had none — a deliberate earlier choice to avoid an ESM/CommonJS conflict, achieved by keeping `renderer.ts` free of `import`/`export` syntax. Toast UI Editor's npm package externalizes its ProseMirror dependencies in a way that only works with a bundler, not a bare `<script>` tag, so this constraint had to be revisited. Verified via a standalone smoke test (loaded the esbuild-bundled editor directly in a browser tab) before building the rest of the feature on top of it, to avoid discovering integration problems after the fact. `esbuild` bundles only `renderer.ts`; `main.ts`/`preload.ts` still compile via plain `tsc`, unaffected. Full detail in `ARCHITECTURE.md` §1.
- **Semester filter added to the course list** (dropdown, persisted via `app_settings`) — the user raised this mid-conversation as a related organizational need while describing their old Notion structure ("semester > course > notes"), so it was built alongside notes rather than as a separate follow-up.
- New `notes:*` IPC surface (list/create/updateContent/updateTitle/delete, native right-click delete menu matching the existing pattern) — `notes` table already existed in the schema from Phase 0, no migration needed.
- `scripts/verify-app.js` extended: creates a note, types live-rendered content via the WYSIWYG editor, confirms autosave, closes/reopens to confirm persistence, deletes it; also covers the semester filter (hide/show, persistence). Note: the WYSIWYG editor's contenteditable region needs `force: true` on Playwright clicks and a WYSIWYG-scoped selector — a second, genuinely-hidden ProseMirror instance for Markdown-source mode matches a bare `[contenteditable]` selector and confuses Playwright's visibility check even though the real one is visibly rendered (confirmed via screenshot during development).

## Session 22 additions

- **Real bug found and fixed: deleting a file directly from Atlas's own managed storage folder left a broken resource behind.** The user reported two stale certificate PDFs still showing in the app (screenshot: "resource not found" on click) after deleting them. Investigation traced it to `Downloads/Atlas-Storage/files/Database Systems/` — the files had been deleted straight from that folder (not from an external watched folder), and Atlas only had `chokidar` watching *external* watched folders, never its own managed storage. `ARCHITECTURE.md` §2 already documents the data directory as deliberately user-browsable/hand-editable, so this was a real gap between the architecture's stated intent and what was actually built, not a new feature request. Fixed by watching every course's own storage folder the same way (`startWatchingCourseStorage()` in `main.ts`, started at app launch for every course and immediately on course creation, stopped on course delete) — a manually-dropped-in file is now auto-imported, and a manually-deleted one is auto-removed, with the same live-`unlink` + startup-reconciliation pattern already used for external watched folders. Confirmed fixed against the user's real data: both stale certificate resources (ids 30/31) were gone after the next launch.
- **View mode (list/icon) is now a persisted, app-wide setting** rather than an in-memory renderer variable that reset to "list" every launch. New generic `app_settings` key/value table; `setViewMode()` writes to it, `init()` reads it before the first render. Deliberately global rather than per-course, per explicit user request ("even the other folder should be icon view").
- Added a real, well-filled `.xlsx` to the user's standing test data (`CS201-gradebook.xlsx` — see session 21) and verified it renders correctly via both in-app preview and "Open in browser."
- `scripts/verify-app.js` extended: drops a file directly into managed storage and confirms auto-import, deletes it directly and confirms auto-removal; switches to icon view, confirms the setting persists in `app_settings`, then does a full app close + relaunch against the same data dir and confirms icon view is still active on the fresh launch (not just that the DB value persists — the actual startup-restore path).

## Session 21 additions

- **Watched-folder deletion now syncs**: deleting a file from a watched folder deletes the `resources` row (and its managed-storage copy) too, both live via `chokidar`'s `unlink` event and retroactively via a `reconcileWatchedFolder()` pass that runs whenever a folder's watcher (re)starts — catches files deleted while Atlas wasn't running, which `unlink` alone can't see. Previously such resources stayed listed and threw "resource not found" when opened.
- **Added a real `.xlsx` to the user's standing test data** — `CS201-gradebook.xlsx` in Data Structures & Algorithms, two sheets (Assignments: 11 rows across homework/quizzes/exams/projects with realistic scores; Grade Summary: weighted category breakdown), generated with the same `xlsx` library Atlas depends on and uploaded via the existing `ATLAS_TEST_UPLOAD_PATH` hook against real storage (same pattern as the original session-11 seed data). Verified it renders correctly both in-app and via "Open in browser."
- **User feedback on PPTX fidelity, logged as `open-questions.md` #12**: after trying "Open in browser" on a real slide deck, the user said the text-only outline is fine for now but they'll likely want the Google Drive route (real slide rendering via Google Slides) once Phase 3 sync work starts anyway, since the Drive OAuth/upload plumbing would already exist at that point — explicitly not something to build now.
- `scripts/verify-app.js` extended: deletes a watched folder's source file mid-run and confirms both the resource entry and its managed-storage copy disappear.

## Session 20 additions

- **"Open in default app" replaced with "Open in browser"**, per explicit user request: they wanted every file type reachable from real, switchable browser tabs instead of separate native-app windows. User initially asked about auto-opening DOCX/PPTX/XLSX in Google Docs/Sheets specifically — flagged before building that this would actually require uploading each file to Google Drive first (Google's viewers can only fetch from a public URL, not a local `file://` path), a real architecture change out of scope right now. User agreed to try a local-only alternative first.
- **New local HTTP server** (`src/main/localServer.ts`), loopback-only (`127.0.0.1`, random port, never network-reachable), started at app launch. `GET /resource/:id` streams PDF/image/text as their native content type (browser renders natively) and reuses the *exact same* `getPreview()` conversion the in-app preview already used for markdown/DOCX/PPTX, wrapped as a standalone HTML page. The right-click menu's "Open in browser" calls `shell.openExternal()` on this URL instead of the old `shell.openPath()` (which launched Word/PowerPoint/etc.). The now-fully-dead `resources:open`/`openResource` IPC path (superseded back in session 9-12 once click-to-preview replaced the old Open button, but never removed) was deleted while touching this area.
- **XLSX support added** — new resource kind, previously not previewable in-app at all. Rendered via `xlsx` (SheetJS) to HTML tables, both in-app and through the local server. **Known caveat, documented in `ARCHITECTURE.md` §7:** the npm `xlsx` package has unpatched prototype-pollution/ReDoS advisories with no newer npm release available (SheetJS moved fixes to their own CDN) — accepted as low-risk since Atlas only parses the user's own local files, never untrusted network input, and formula evaluation is disabled on read. Worth revisiting if spreadsheets ever come from an external/untrusted source later.
- **Found and fixed a real pre-existing bug** while checking the PPTX-in-browser output: `extractPptxOutline()`'s text extraction pulls raw text straight from the slide XML, which is already XML-entity-escaped (a literal `&` is stored as `&amp;`); the existing `escapeHtml()` call was re-escaping it into `&amp;amp;`, which browsers display as the literal text `&amp;` instead of `&`. Fixed with a `decodeXmlEntities()` step before `escapeHtml()`. Pre-existed since the PPTX preview was first built (session 10) but wasn't visibly caught before, since in-app preview testing hadn't included a slide with an ampersand.
- `scripts/verify-app.js` extended: fetches the local server directly (native context menu can't be automated, same limitation as delete) for a markdown resource, a freshly-generated real `.xlsx` (built with the same `xlsx` library, not just a byte fixture) confirming it renders as an HTML `<table>`, and an image resource confirming raw `image/png` content-type. While adding this, hardened three spots in the script that previously grabbed "whatever's newest in the DOM" (`.resource-name >> nth=0`) to instead capture the real resource ID directly from `uploadResource()`'s return value — SQLite's `datetime('now')` is only 1-second resolution, so near-simultaneous uploads can tie on `added_at` and make DOM order unreliable (this caused one real flaky failure while writing the new tests, now fixed at the root rather than papered over with a longer wait).

## Session 19 additions

- **Local folder watching built** — the next Phase 1 item per the session-18 note below. Folder→course mapping decided as explicit-only (not auto-guessed) rather than asking the user to confirm mid-session, on the reasoning already flagged as the leaning in session 18 and consistent with the "Atlas owns data, Claude owns reasoning" rule — logged as a resolved decision in `open-questions.md` #11 rather than silently picked with no record.
- New `watched_folders` table (`course_id`, `folder_path` UNIQUE) and a new `resources.watch_source_path` column (migration added for existing DBs). One `chokidar` watcher per watched folder, managed in `main.ts` (`activeWatchers` map); started for all rows at app launch and for any newly-added folder; stopped on "Stop watching" and when its course is deleted (cascade-delete alone would've left an orphaned live watcher).
- Manual upload's copy-into-storage + insert-resource logic was factored out into a shared `importFileIntoCourse()` so watched-folder imports go through the exact same path (same filename handling, same collision-disambiguation) instead of a parallel implementation.
- UI: each course's Resources section now has a "Watched folders" list with a "Watch a folder…" button (native directory picker); right-click a watched folder → "Stop watching" (same right-click-only pattern as course/resource delete, per the CLAUDE.md UI convention already established). The main process pushes a `resources:changed` event to the renderer when a watcher imports a file, so the resource list refreshes live if that course is open — not just on next manual reselect.
- `scripts/verify-app.js` extended: watches a temp folder that already contains a file (confirms pre-existing files are picked up, not just future ones), then drops a second file in while the watcher is live (confirms ongoing detection), both via the same `ATLAS_TEST_WATCH_FOLDER_PATH` env-var test-hook pattern already used for uploads. Full suite passes.

## Session 18 additions

- **The actual bug behind the repeated zoom-scoping reports, finally found**: `#zoom-controls { display: flex; ... }` set `display` unconditionally on an ID selector, which outranks the browser's default `[hidden] { display: none }` rule — so even though the JS logic correctly set `zoomControls.hidden = true` for every non-image type, the CSS kept it visibly rendered anyway (user saw it on a `.txt` file). This is the exact same class of bug already hit and fixed twice before, for `#preview-overlay` (session 10) and `#confirm-overlay` (session 11) — should have caught this one at the same time; noted for future `[hidden]`-toggled elements to always double-check for an unconditional `display` on their own ID selector. Fixed with `#zoom-controls:not([hidden]) { display: flex; }`. Audited the rest of the stylesheet for the same pattern — nothing else affected (the other `display: flex` rules found belong to descendants of already-correctly-hidden ancestors, not elements toggled via `.hidden` themselves).
- `scripts/verify-app.js` now explicitly asserts `#zoom-controls` stays hidden for both a markdown and a `.txt` preview (the exact reported case) — regression-proofed going forward.

## Session 17 additions

- **Found and fixed the actual remaining leak** behind the user's repeated "scaling doesn't work / applies to other file types" report: session 15's scoping only covered the JS `applyImageZoom()` lookup and the zoom-controls *visibility* — two spots still used the generic, unscoped `#preview-body img` selector: (1) the CSS rule setting `max-height`/`transform-origin`/`transition`, which applied to *any* image including ones embedded in rendered docx/pptx/markdown HTML, and (2) the Ctrl+scroll wheel handler's guard check. Both now scoped strictly to `img.preview-image` (the dedicated class on the standalone image-preview `<img>`); embedded images inside `.preview-html` get a separate, transform-free `max-width: 100%` rule. Zoom (buttons, Ctrl+scroll, and its CSS) now provably cannot touch anything but a real image-kind resource.

## Session 16 additions

- **Two persistent test courses seeded** into the user's real `Downloads/Atlas-Storage`, replacing the earlier single "Test Course": **Data Structures & Algorithms** (CS201, Monsoon 26 — the original sorting-algorithms set) and **Database Systems** (CS305, Spring 27 — a new normalization/joins/indexing set), 7 sample resources each (pdf, docx, pptx, image, txt, md, zip), all realistic and multi-page/multi-slide. This is the user's standing test data going forward — leave it alone unless told otherwise, same as before.
- Confirmed for the user: the Desktop shortcut / `Launch Atlas.bat` always rebuilds (`npm start` = `npm run build && electron .`) before launching, so any code change is picked up automatically on next launch — no separate "deploy" step exists or is needed at this stage.

## Session 15 additions

- **Zoom scoping tightened**: the `<img>` created for a standalone image preview now gets a dedicated `.preview-image` class, and all zoom logic (`applyImageZoom`, the wheel handler) targets `#preview-body img.preview-image` specifically — not just any `<img>`, which could otherwise have matched an image embedded inside rendered docx/pptx/markdown HTML. Zoom UI was already scoped to `preview.type === 'image'` only (PDF/other types never showed it); this closes a latent cross-contamination risk rather than fixing an observed bug — the user's report was almost certainly just PDF's built-in Chromium zoom not being ours (expected — iframe content is a separate document, our wheel handler can't reach it).
- **Per-resource zoom is now remembered** — new `resources.zoom_level` column (migration added for existing DBs), set via `resources:setZoom` IPC whenever the user changes zoom (buttons or Ctrl+scroll), and restored automatically the next time that exact resource is previewed. Verified end-to-end in `scripts/verify-app.js` (zoom to 150%, close, reopen, confirm still 150%).
- **Real "Test Course" and its 7 seeded files deleted** from the user's actual `Downloads/Atlas-Storage`, per explicit request — confirmed `files/` is now empty.
- **One-click launcher added**: [`Launch Atlas.bat`](../Launch%20Atlas.bat) in the repo root (`cd`s to itself, runs `npm start`) plus an `Atlas` shortcut on the user's Desktop pointing to it (using Electron's own icon). Both tested working. Documented in `README.md` "Running it."
- **Open question raised, not yet resolved**: the user's ask "remember the scaling for that exact file" used PDFs as the motivating example, but Chromium's built-in PDF viewer (used for PDF preview) runs in an isolated iframe with no scripting API we can read live zoom from — only a one-way `#zoom=N` URL-fragment hint is possible (sets initial zoom, can't observe what the user changes it to afterward, and would need its own separate control since Ctrl+scroll can't reach into the iframe). Implemented per-resource zoom memory for **images only** this round; PDF zoom-memory needs a decision from the user on whether that one-way, reload-based approach is worth building — see `open-questions.md`.

## Session 14 additions

- **Images now center both horizontally and vertically** in the preview panel (`#preview-body.centered`, flex-centered), not just horizontally as before.
- **Zoom controls added for image previews**: −/+/Reset buttons plus Ctrl+scroll-wheel (`#zoom-controls`, 25%–400% range, 25% steps), implemented via CSS `transform: scale()` on the `<img>`. Zoom resets to 100% every time a new preview opens. Not added for PDF — Chromium's built-in PDF viewer already has its own zoom controls (per the user's own observation).
- **`scripts/verify-app.js` now uploads two resources** (markdown + a tiny embedded-base64 test PNG) and covers the zoom/centering behavior end-to-end. Caught and fixed a real bug in the test script itself while adding this: the resource-delete step grabbed "whatever `.resource-name` is first" to get an ID, which broke once a second (newer) upload sorted before the markdown one — fixed by capturing the markdown resource's ID right after its own upload instead of relying on list order later.
- User decided to hold off on giving UI direction until more features are built; current visual state is considered acceptable for continued feature work in the meantime.

## Session 13 additions

- User confirmed delete (via right-click) works correctly in real use.
- **Fullscreen preview chrome tightened** — header bar and padding shrink significantly in fullscreen mode (was still leaving a lot of unused border/padding around the content).
- **Fullscreen/Close buttons are now monochrome icon buttons** (inline SVG, `stroke="currentColor"`) instead of text labels — expand/compress icon toggles with fullscreen state, X for close. No icon library added; these are small inline SVGs directly in `index.html`/`renderer.ts`.
- **UI direction question raised and answered**: recommended giving a rough visual direction now (palette/density/reference feel) rather than waiting until all features are built, to avoid restyling scaffolding twice — but held off on a full design pass until Phase 1 features are more complete. Still waiting on the user's actual direction as of this update.

## Session 12 additions

- **Add-course form now sits above the course list** (was below).
- **Fullscreen toggle added to the preview panel** (`#preview-fullscreen` button) — expands the panel edge-to-edge; resets to non-fullscreen every time the preview is closed and reopened.
- **All visible Delete buttons removed** — deleting a course or resource is now right-click-only (native Electron `Menu`, same as "Open in default app" already was for resources). Added the equivalent native context menu for courses (`resources:courseContextMenu` IPC in `main.ts`, `showCourseContextMenu`/`onCourseContextMenuDelete` in preload). The in-app confirm modal (session 11) still gates both.
- **Test coverage tradeoff, worth knowing:** since delete is now exclusively behind a native OS context menu, `scripts/verify-app.js` can no longer click a "Delete" button to test the flow — Playwright cannot drive native menus. It now calls `window.atlas.deleteResource()`/`deleteCourse()` directly (the same underlying API the menu's "Delete" item calls) to verify the delete + list-refresh logic, but the actual right-click → menu → click interaction is **not covered by the automated suite** and needs a manual check from the user occasionally.
- User asked to leave the seeded "Test Course" (in their real `Downloads/Atlas-Storage`) alone for now while they test — noted, will only delete/modify on explicit instruction. (`Verify Script Test Course` is unrelated — that name only ever exists transiently inside `scripts/verify-app.js`'s throwaway temp directories, never in real storage.)

## Session 11 additions

- **Native `window.confirm()` replaced with an in-app modal** (`#confirm-overlay` in `index.html`, `showConfirm()`/`resolveConfirm()` in `renderer.ts`) — styled consistently with the rest of the UI instead of an OS-native dialog box. Same `[hidden]`-vs-ID-selector CSS specificity trap as the preview overlay (see session 10) applied here too; fixed the same way (`#confirm-overlay:not([hidden])`). `scripts/verify-app.js` updated to click `#confirm-yes`/`#confirm-cancel` instead of relying on Playwright's native-dialog auto-accept, and now also covers the Cancel path (resource must survive if Cancel is clicked).
- **Real "Test Course" seeded into the user's actual `Downloads/Atlas-Storage`** with 7 sample resources — one of every supported kind, multi-page/multi-slide with realistic academic content (a sorting-algorithms lecture theme, consistent across files): a 5-page PDF (via `reportlab`), a multi-page DOCX with headings/lists/a code snippet (via `python-docx`, explicit page breaks), an 8-slide PPTX (via `python-pptx`), a generated PNG complexity chart, a plain-text raw-notes file, a Markdown study guide (table, code block, list), and a ZIP with a few starter files inside. Generated with Python (`python-docx`, `python-pptx`, `Pillow`, `reportlab` — installed via pip for this one-off generation task, not a project dependency) and uploaded into the real app via the existing `ATLAS_TEST_UPLOAD_PATH` test hook (same upload code path a real user upload takes, just skipping the native file-picker dialog). This is real seeded data in the user's actual storage, not a throwaway test — it's meant to stay there for them to click through.

## Session 10 additions

- **In-app resource preview built** (`ARCHITECTURE.md` §7): click a filename to open a preview overlay — PDF/image render natively, markdown renders via `marked` (pinned to v12, since v13+ dropped the CJS build our CommonJS main process needs), DOCX converts to HTML via `mammoth`, PPTX gets a text-only slide outline (no free library renders real slide layout, so this is a deliberate scope line, not a bug — see `ARCHITECTURE.md` §7 for the full reasoning). Right-click any resource for a native context menu with "Open in default app" (the old separate "Open" button is gone — replaced by click-to-preview, matching how a real file manager behaves).
- **List/icon view toggle** added above the resource list.
- **Two real bugs hit and fixed while building this:**
  1. `marked@18` (latest at install time) is ESM-only with no CJS export condition — `require('marked')` in the CommonJS main process threw `ERR_REQUIRE_ESM` at startup. Tried a dynamic `import()` as a workaround, but TypeScript (with `module: commonjs`) downlevels dynamic imports back into a `require()` call anyway, so it didn't help. Fixed by pinning to `marked@^12.0.2`, the last major with a working CJS build.
  2. The preview overlay's CSS set `display: flex` directly on `#preview-overlay` (an ID selector), which outranked the browser's default `[hidden] { display: none }` rule — so the "hidden" overlay was actually still visible and intercepting clicks. Fixed with `#preview-overlay:not([hidden]) { display: flex; }` instead.
- Manually verified (outside the permanent test suite, via throwaway fixture files) that DOCX and PPTX preview actually produce correct output — a minimal fixture `.docx` and `.pptx` were built with Python's `zipfile` and run through the real app; both rendered as expected. `scripts/verify-app.js` itself only exercises the markdown path end-to-end (safe to click in an automated run) plus the view toggle; it does not click "Open in default app" for the same reason as before — that would launch a real external OS application during an automated run.
- **Fixed doc drift**: `ROADMAP.md` Phase 2 still said "OCR pipeline (Google Cloud Vision)" from before the OCR decision changed to local Tesseract.js — missed in an earlier session, caught and fixed now.

## Session 9 additions

- **Course folders are now named after the course** (e.g. "test123"), not `course-<id>`. `courses.folder_name` is a new column, computed once at course creation (sanitized for filesystem-invalid characters, disambiguated with `(2)`, `(3)`... on collision) and stored — deliberately *not* recomputed from the course name later, so a future rename feature won't silently break already-stored file paths. Existing dev databases get this column added automatically via a small migration in `src/main/db/database.ts` (`PRAGMA table_info` check + `ALTER TABLE`).
- **Uploaded files keep their original filename** instead of a timestamp prefix — only disambiguated (`name (2).ext`) if that exact filename already exists in the course's folder.
- **"Open" button added per resource** — uses `shell.openPath()` to open the file with the OS's default application. This is the PRD §10 fallback path ("if native viewing is unavailable, open the default external application"), not the proper in-app PDF/image/markdown viewer, which is still a separate upcoming Phase 1 item. Deliberately *not* exercised by `scripts/verify-app.js` (only checked for presence in the DOM) — actually invoking it would launch a real external OS application during an automated test run, which isn't safe/appropriate to trigger from a script.

## Session 8 additions

- **Course term is now a fixed dropdown**, not free text: Monsoon 26, Spring 27, Monsoon 27, Spring 28 (user-specified list, in `src/renderer/index.html`). If more terms are needed later, add `<option>`s there.
- **Delete added for both courses and resources**, needed for the user to freely test/clean up without leftover data piling up. Deleting a course removes its entire `files/course-<id>/` folder from disk and cascades to delete its `resources` rows (FK `ON DELETE CASCADE`, `foreign_keys` pragma is on). Deleting a single resource removes just its file and row. Both prompt a native `confirm()` before proceeding. `scripts/verify-app.js` now covers both delete paths and auto-accepts the confirm dialog (`window.on('dialog', ...)`, since Playwright auto-dismisses dialogs by default otherwise).

## What's next (updated session 23 — supersedes the session-19 note below, kept for history)

Notes UI (previously "proposed next") is now built — see session 23 above.

Remaining Phase 1 items, no particular order yet: dashboard v1 (upcoming deadlines, recently added resources, "what changed today"), global search UI (FTS5 `search_index` table exists, still unpopulated — notes/resources aren't indexed into it yet, worth deciding when building search whether to backfill retroactively).

<details>
<summary>Superseded — session 19 note</summary>

Local folder watching (previously "proposed next") is now built — see session 19 above.

Remaining Phase 1 items, no particular order yet: notes UI (format still open, `open-questions.md` #1), dashboard, global search UI (FTS5 table exists, unpopulated).

</details>

<details>
<summary>Superseded — original session 18 note</summary>

Proposed next: **local folder watching** (chokidar) — reuse the same "insert into resources" logic manual upload already has; the new piece is (a) watching user-designated folder(s) for new files and (b) deciding which course a detected file belongs to (leaning toward explicit user-configured folder→course mapping over auto-guessing, but this wasn't confirmed with the user yet — ask before building). Not started as of session 18.

</details>

**Open question needing the user's decision** (not blocking, but don't build silently): whether to implement one-way "set an initial zoom via a separate control" for PDF previews, given Chromium's built-in PDF viewer can't be read from/written to live — see `open-questions.md` #10.

User has said to commit/push continuously without waiting for approval in this repo (see `CLAUDE.md` "Git workflow") — keep doing that.

<details>
<summary>Superseded — original session 7 note</summary>

**Manual file upload is done and verified** (self-tested via `npm run verify`, screenshot confirmed): click a course to select it, "Upload file" opens a native file picker, the file is copied into `Downloads/Atlas-Storage/files/course-<id>/` with a timestamp prefix (collision-safe), a `resources` row is inserted with `kind` auto-detected from file extension, and the resource list re-renders under the selected course. `main.ts` has an `ATLAS_TEST_UPLOAD_PATH` env-var test hook so `scripts/verify-app.js` can drive the upload without needing to interact with the native OS file dialog (which Playwright can't click into).

Remaining Phase 1 work, roughly in order: local folder watching (chokidar) — reuse the same "insert into resources" logic as manual upload; resource viewer (PDF/image/markdown/text, falling back to the OS default app); notes UI (typed notes, format still open per `open-questions.md` #1); dashboard; global search UI wired to the existing FTS5 `search_index` table (not populated yet — decide when building search whether to backfill it retroactively for resources added via upload/folder-watch).

(Note: resource viewer and folder-naming details in this superseded note are now out of date — see "Where things stand" above for current reality.)

</details>
